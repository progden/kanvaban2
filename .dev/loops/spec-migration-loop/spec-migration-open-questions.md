# spec 遷移待決事項（OQ）

遷移過程中遇到的**高影響假設**（見規則書「假設分級」）。loop 不停下等人：執行輪選定「最小驚訝、與既有 Scenario 一致」的選項照做，並在下表末尾追加一列；對應 spec 的 `## 待釐清` 會有一行指向它。人工事後檢視，不同意的再開 CR 或 D-xx 處理。

規則（由 `verify-spec-migration.sh` 檢查）：

- **只能在表格末尾追加列**，不可修改或刪除既有列，表格之後不可再加其他內容。
- 編號 `OQ-01` 起流水號；「採用」寫實際採用的選項，「依據」寫依據哪個 Scenario（模組、Feature、Scenario 名稱），兩欄都不可空白。
- 狀態：`自動決議`（loop 已照採用的選項做）／`blocked`（環境限制，對應任務已標 blocked）。

| 編號 | 日期 | 模組 | 情況 | 選項 | 採用 | 依據 | 狀態 |
|---|---|---|---|---|---|---|---|
| OQ-01 | 2026-09-16 | F01 | 「該操作應該被記錄為活動紀錄」在 usecase 區塊要如何表達：另立一個 log 類 uc、用 emits 事件讓別的 uc requires 它，還是併入原 uc 的 post 與 crud | (a) 另立 uc-log-activity，其他 uc emits 它；(b) 併入各 uc 自身 post，並在 crud 加上所屬 board 的 U，Aggregate 註解標 board: read, write | (b)：Swimlane 管理、Stage（階段）管理共 9 個 uc 的 crud 補上 board: U，成功 Scenario 的 Aggregate 註解補回 board: read, write；不另立 uc、不用 emits/requires | 依據 F01「重新命名 Swimlane」「新增一個 Stage」等 9 個 uc 的成功 Scenario（步驟「該操作應該被記錄為一筆活動紀錄」），以及 F02 legacy「檢視看板活動紀錄」Feature 的實作備註：Board／Card 既有事件的操作人記錄各自記在 `Board.activityLog`／`Card.activityLog`，代表活動紀錄屬於被操作的 Aggregate 本身，不是獨立實體 | 自動決議 |
| OQ-02 | 2026-09-16 | F01 | 「刪除卡片」Feature 的「取消刪除卡片」Scenario（使用者於確認訊息中選擇取消，卡片不被刪除）該如何歸入 uc-delete-card 的 usecase 區塊：當成獨立的成功 Scenario（要求 Aggregate 註解標 card: write，但實際未寫入，與腳本 GH-06 及事實不符），還是視為一種「失敗」分支（不完成刪除） | (a) 保留為無 @fail- 的成功 Scenario，card 註解維持 read（GH-06 會報 crud.card=D 但未標 write）；(b) 在 pre 補一條「使用者於確認訊息中選擇取消」，fail 補對應句「拒絕，該 card 不被移除，資料不變」，Scenario 掛 @fail-p2 | (b)：uc-delete-card 的 pre 新增 p2、fail 新增 p2，「取消刪除卡片」Scenario 改掛 `@uc-delete-card @fail-p2`，Aggregate 註解維持 card: read 不變 | 依據 F01「Card（卡片）編輯」Feature 的「取消刪除卡片」Scenario：Given 存在一張卡片、When 點擊刪除、And 在確認訊息中選擇取消、Then 該卡片應該仍然存在於看板中——步驟本身即「使用者選擇取消、資料不變」，與 fail 分支「拒絕，資料不變」的既有措辭（見 uc-delete-swimlane／uc-delete-stage）語意一致 | 自動決議 |
| OQ-03 | 2026-09-16 | F01 | OQ-02 採用內容中 pre.p2 措辭方向寫反：原文把 p2 寫成「使用者於刪除 card 的確認訊息中選擇取消」，導致「刪除卡片需要確認」這個成功 Scenario（使用者確認刪除）反而不滿足 pre.p2，「取消刪除卡片」的 fail 分支反而滿足 pre | (a) 維持原文不動；(b) p2 改為「使用者於刪除 card 的確認訊息中確認刪除」，fail.p2 改為「不刪除，該 card 仍存在於看板中，資料不變」，post 第一句改為「該 card 從看板中移除」並補上活動紀錄句 | (b)：p2 改為確認刪除、取消為 p2 不成立，fail.p2 與 post 一併修正 | 依據 F01「Card（卡片）編輯」Feature 的「刪除卡片需要確認」Scenario（When 我確認刪除 → Then 該卡片應該從看板中移除，且該操作應該被記錄為一筆活動紀錄）與「取消刪除卡片」Scenario（And 我在確認訊息中選擇取消 → Then 該卡片應該仍然存在於看板中） | 自動決議 |
| OQ-04 | 2026-09-16 | F02 | 角色定義依「Owner／Member 權限有寫明才分角色，否則整個模組一個角色」拆分：「Board 建立與成員邀請」「Board 權限管理」兩個 Feature 明確寫出 Owner 專屬權限（邀請／移除／升級成員、調整看板結構、刪除看板），需要獨立角色；但「卡片負責人指派」「檢視看板活動紀錄」「Board 存取權限」三個 Feature 的原標頭寫「身為看板的成員」／「身為 Board 的成員」，且情境（例如「檢視看板活動紀錄」的 Background 是 Owner 執行）並未限定只有 Member 可做，若直接套用「Board 成員（r-board-member）」角色會誤將 Owner 排除在外 | (a) 沿用原文「成員」字面，改用 r-board-member 角色（隱含只有 Member 能做，與 Background 用 Owner 示範矛盾）；(b) 這三個 Feature 改用新設的通用角色 r-system-user（已登入的一般使用者），與帳號建立／登入登出共用；r-board-owner、r-board-member 只用於權限有明確差異的兩個 Feature | (b)：新增 r-system-user 供「建立使用者帳號」「使用者登入與登出」「Board 存取權限」「卡片負責人指派」「檢視看板活動紀錄」五個 Feature 共用；r-board-owner／r-board-member 只用於「Board 建立與成員邀請」「Board 權限管理」 | 依據 F02 legacy「檢視看板活動紀錄」Feature 的 Background「我是 Board「產品開發看板」的 Owner」與 Scenario 未限制角色的行為；以及「Board 權限管理」Feature 明確寫出 Owner／Member 權限差異（Member 無法邀請成員、無法變更角色、無法調整看板結構，但可以新增與編輯卡片） | 自動決議 |
| OQ-05 | 2026-09-16 | F02 | OQ-04 沒有考慮沿用 F01 已定義的 r-user（看板使用者），另外新設 r-system-user 涵蓋「指派卡片負責人、檢視看板活動紀錄」，違反階段 2「角色跨模組共用」的原則；且 r-system-user 的說明把「指派卡片負責人、檢視看板活動紀錄」寫成該角色的權限，但原文從未賦予這兩件事給「系統使用者」這個層級，是遷移時誤加的敘述 | (a) 維持 OQ-04 的做法，r-system-user 涵蓋五個 Feature；(b) 「卡片負責人指派」「檢視看板活動紀錄」改沿用 F01 已定義的 r-user（看板使用者），F02 角色表不重列 r-user；r-system-user 的說明改為只涵蓋建立帳號、登入登出、檢視自己有權限的 Board，移除「指派卡片負責人、檢視看板活動紀錄」；「Board 存取權限」維持「身為 系統使用者」 | (b)：「卡片負責人指派」「檢視看板活動紀錄」兩個 Feature 標頭改為「身為 看板使用者」，F02 角色表不重列 r-user；r-system-user 說明移除「指派卡片負責人、檢視看板活動紀錄」；「Board 存取權限」維持「身為 系統使用者」 | 依據 F01 角色表 r-user「可操作看板（Swimlane、Stage）與卡片的一般使用者」；F02 legacy「卡片負責人指派」原標頭「身為看板的成員」（指派負責人是操作看板上的卡片）；F02 legacy「檢視看板活動紀錄」原標頭「身為 Board 的成員」與其 Background 由 Owner 示範（檢視活動紀錄不限定 Owner／Member，屬一般看板使用者可做的操作） | 自動決議 |
| OQ-06 | 2026-09-16 | F02 | 「Board 權限管理」Feature 的 5 個 Scenario（3 個 Member 被拒絕操作、1 個刪除 Board、1 個 Member 可新增卡片）該如何歸入 usecase：GH-01 規定 Scenario 的 `@uc-` 必須指向同一個 Feature 的 usecase 區塊，UC-06 規定每個 uc 至少要有一個成功（無 `@fail-`）Scenario，所以無法沿用「Board 建立與成員邀請」的 uc-invite-member／uc-change-member-role，也無法沿用 F01 的 uc-add-swimlane／uc-add-card；3 個純拒絕情境沒有對應的成功 Scenario | (a) 在「Board 建立與成員邀請」與 F01 補上這些 Scenario 對應的 fail 分支，把 Scenario 移過去或跨 Feature 引用（違反 GH-01，且移動 Scenario 違反鐵則 1）；(b) 在本 Feature 新立 5 個專屬 uc（uc-reject-invite-by-member、uc-reject-role-change-by-member、uc-reject-structure-change-by-member、uc-delete-board、uc-member-add-card），3 個純拒絕 uc 的 crud 全為 R、roles 留空（沿用原 Aggregate 註解只有 read 的事實），uc-delete-board 只在 pre 記錄「操作者是 Owner」、不列 fail（比照 F01 uc-rename-swimlane 等「有 pre 無 fail」的既有寫法），uc-member-add-card 的 roles 用 r-board-member（F02 角色表已明定 Member 可新增卡片） | (b)：新立上述 5 個 uc，全部只在本 Feature 的 usecase 區塊內 | 依據本 Feature 5 個 Scenario 各自的 Given／When／Then；3 個拒絕情境的 Aggregate 註解原本就只標 read，無寫入；F02 角色表 r-board-member「可以正常新增／編輯／移動／刪除卡片」；F01「Swimlane 管理」uc-rename-swimlane／uc-reorder-swimlane 的 pre 非空但 fail 留空的既有先例 | 自動決議 |
| OQ-07 | 2026-09-16 | F02 | OQ-06 讓「Board 權限管理」3 個拒絕類 uc（uc-reject-invite-by-member、uc-reject-role-change-by-member、uc-reject-structure-change-by-member）的 roles 留空，看不出被拒絕的操作者是誰；同 Feature 的「Board 存取權限」uc-reject-board-access-by-nonmember 也留空 | (a) 維持留空；(b) 依 Background／Feature 標頭填上被拒絕的操作者角色：前三者改為 `roles: [r-board-member]`，uc-reject-board-access-by-nonmember 改為 `roles: [r-system-user]` | (b)：4 個拒絕類 uc 依上述規則補上 roles | 依據「Board 權限管理」Background「"雅婷" 是這個 Board 的 Member」，以及「Member 無法邀請其他成員」「Member 無法移除或升級成員」「Member 無法新增、重新命名或刪除 Swimlane 與 Stage」三個 Scenario 皆由 "雅婷" 操作；「Board 存取權限」Feature 標頭「身為 系統使用者」 | 自動決議 |
