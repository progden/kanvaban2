# 安排階段 現況（`_planning`，每輪覆寫）

- 最後一次安排：2026-09-22（驅動腳本「無可執行任務」時自動重跑）。
- 任務總數 24（原 23 ＋ 本輪新增 1 列修訂實例）；狀態：12 `done`、12 `todo`、0 `blocked`、0 `doing`／`review-pending`。
- 本輪變動：(1) 23 列既有任務備註欄補上「涵蓋 CR：…」；(2)「規則」段補上備註欄格式與修訂實例 `-rN` 兩條；(3) 新增 `T-08-be-feature-cr-board-r2`（CR-013，F06 程式碼落後 spec）。
- 本輪沒有改動任何既有 `status` 檔，沒有新增／解除 `blocked`，沒有開立 `OQ-PLAN-xx`。
- CR 落後檢查結果：CR-006／007／008／009／010／011／012 的程式碼都已跟上（見 `decision-log.md` 決策 1 的逐筆時序佐證）；只有 CR-013 未落地。
- 下一輪驅動腳本可平行啟動的任務（依賴皆 `done` 且已合併，`MAX_PARALLEL=5` 名額足夠）：
  - `T-07-be-workload`（依賴 T-03、T-04）
  - `T-08-be-feature-cr-board-r2`（依賴 T-08）
  - `T-09-be-canvas-layout`（依賴 T-02、T-04）
  - `T-12-fe-board-list`（依賴 T-10、T-02、T-04、T-22）
- 仍被依賴卡住的：`T-13-fe-canvas-shell` 等 T-12／T-09；`T-14`～`T-20` 等 T-13（加上各自的後端任務）。前端剩餘任務實際上是一條以 T-12 → T-13 為瓶頸的鏈。
