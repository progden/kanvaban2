# T-13-fe-canvas-shell 修正任務（D-xx）

> 由 `loopctl fix add`／`fix done` 維護，不要直接編輯。

## 第 1 輪 Review 退回

| ID | 回合 | 狀態 | 延續 | 描述 |
|---|---|---|---|---|
| D-01 | 1 | done | — | 掛載點把 `item.component` 當成 item 識別碼傳給註冊的元件，下游任務拿不到 item 的 id。 - `kanban-frontend/src/canvas/itemComponentRegistry.tsx` 宣告 `ItemContentProps { itemId: string; width; height }`。 - `kanban-frontend/src/canvas/CanvasStage.tsx:428` 實際傳的是 `<Content itemId={item.component} width={box.width} height={box.height} />`，也就是元件識別碼（例如 "board"），不是 `item.id`。 本任務在 `tasks.md` 的產出範圍就是「提供給其他前端任務掛載自己的 item 內容」，這個 prop 是掛載點的對外契約。T-14～T-20 用 `registerItemComponent` 註冊後，同一個 `item.component` 若在同一畫布放了兩個 item，兩邊拿到的 `itemId` 會一樣，無法區分是哪一個 item（spec-canvas-layout.md「item」說明：「某個元件在畫布上的一次放置」，同一元件可以放多次）。 怎樣才算修好：`Content` 收到的 `itemId` 是 `item.id`；`PlaceholderContent` 的文字不再拿識別碼當人看的字串（或另外傳 `component` prop）；補一個測試斷言註冊的元件收到的 `itemId` 等於該 item 的 id。 |
| D-02 | 1 | done | — | 畫面層序直接把 `item.z` 當 CSS z-index 用，造成三個在瀏覽器上實際會發生的顯示錯誤（jsdom 不做繪製與命中測試，現有測試驗不出來）。 `kanban-frontend/src/canvas/CanvasStage.tsx:415`：`const zIndex = item.anchor === 'screen' ? 100000 + item.z : item.z;`，而 `CanvasStage.css` 裡 `.canvas-add-button`、`.canvas-toolbar`、`.canvas-viewport-controls` 都沒有 z-index（＝auto），`.canvas-stage` 是 `position: relative` 且沒有 z-index，不構成 stacking context。 1. `item.z` 為 0 或負值時元素會被畫到 `.canvas-stage` 的格線背景後面，完全看不見。負 z 在正常操作下就會出現：`CanvasApplicationService.reorderItem()` 置底時 `newZ = 其他元素最小 z - 1`，兩個元素各置底一次就會得到 0、-1。 2. 預設畫布上 `uc-init-canvas` 建立的 item 是 (0,0) 900×600、z 為 1，會整個蓋住 `.canvas-add-button`（left:14, top:14，z-index auto），「＋ 加入元件」在真實瀏覽器點不到，`uc-place-item` 無法觸發。 3. 選取後的浮動工具列 `.canvas-toolbar` 同樣是 z-index auto，會被 z-index ≥ 1 的 item 蓋住；而且它的 `top` 是 `item 的螢幕 top - 42`，選取 y 為 0 的預設 item 時算出 -42，在 `overflow: hidden` 的 stage 裡直接被裁掉看不見——ui-canvas-layout.md 驗收條件要求「移動、調整大小、設定能力、設定錨定方式、調整層序後…分別觸發 uc-move-item／uc-resize-item／uc-set-item-capabilities／uc-set-item-anchor／uc-reorder-item」，這些操作的唯一入口就是這個工具列。 怎樣才算修好：畫面 z-index 改用「同一 anchor 內依 `item.z` 排序後的相對名次」之類的非負值（畫面固定元素仍整組高於畫布元素，符合 spec「畫面固定元素永遠繪於畫布元素之上」），或讓 stage 自成 stacking context 並把 chrome（加入元件按鈕、浮動工具列、縮放控制）放到明確高於所有 item 的層；浮動工具列位置要被夾在 stage 可視範圍內。並補測試：`item.z` 為 0／負值的元素仍渲染於背景之上、工具列與「＋ 加入元件」的 z-index 高於全部 item、選取 y=0 的 item 時工具列的 top 不為負。 |
| D-03 | 1 | done | — | `s-canvas` 缺少 ui-canvas-layout.md「資料」表明訂為「輸入」的兩組欄位。 ui-canvas-layout.md 逐字： - 『｜ 元素位置 ｜ `item.x`／`item.y` ｜ 顯示 / 輸入（放置、移動、批次移動、調整大小、設定錨定方式時） ｜ 必填 ｜ …』 - 『｜ 元素能力 ｜ `item.movable`／`item.resizable`／`item.removable` ｜ 顯示 / 輸入（放置、設定能力時） ｜ 布林，預設皆為 true ｜ …』 實作現況： - `kanban-frontend/src/canvas/PlaceItemDialog.tsx` 只有「元件識別碼／寬／高／錨定方式」，`x`、`y` 寫死 `x: 0, y: 0`，也沒有任何能力欄位——放置時無法輸入位置與能力。 - `kanban-frontend/src/canvas/CanvasStage.tsx` 的 `handleToggleLock()` 只把 `movable`／`resizable` 一起切換，`removable` 原封傳回；整個畫面沒有任何地方可以輸入 `item.removable`，也沒有顯示三項能力各自的狀態。 （`decision-log.md` 判斷第 3 點說「鎖定」同時切換 `movable`／`resizable`、`removable` 由後端端點另設 — 但後端端點不是畫面，不能代替 ui 資料表要求的畫面輸入。） 怎樣才算修好：`PlaceItemDialog` 補上 `x`／`y` 與三項能力的輸入欄位（預設值依 spec：anchor 預設 canvas、三項能力預設 true）；畫面上要能分別設定／看到 `item.movable`／`item.resizable`／`item.removable`（浮動工具列多一個能力設定入口即可，沿用既有樣式）；各補一個測試，斷言送出的 `uc-place-item`／`uc-set-item-capabilities` 請求帶到這些值。 |
| D-04 | 1 | done | — | Dev 在 `decision-log.md` 判斷第 1 點自行裁決了兩份文件的矛盾，但沒有依 review／dev 提示詞的規則用 `loopctl oq add` 開 OQ，矛盾只留在決策紀錄裡，人工看不到。 矛盾的兩處原文： - `.dev/F07-canvas-layout/spec-canvas-layout.md`「角色定義」：『｜ r-canvas-editor ｜ 畫布編輯者 ｜ 可放置與排列畫布元素，並平移與縮放自己的檢視區；對應該 Board 的 `board-membership.role` 為 Owner 或 Member 的使用者（即 `.dev/F02-user-membership/spec-user-membership.md` 的 `r-board-owner`／`r-board-member`） ｜』 - `.dev/F07-canvas-layout/ui-canvas-layout.md`「待確認事項」：『- ⚠️ `r-canvas-editor`／`r-canvas-viewer`／`r-board-owner`（F02）三者的對應關係 spec 未定義，見 OQ-44』 採用 spec 的裁決本身沒問題（引用方向 `spec ← ui`，且照做不需要改任何定稿文字），屬於「等級高、不阻塞、照做並開 OQ」的情況，但 OQ 一定要開出來，人工才知道 `ui-canvas-layout.md` 的待確認事項與角色表（含 `r-board-owner` 那一列）已經過時、需要一併修訂。 怎樣才算修好：執行 `loopctl oq add --level 高 --blocking no --owner 人工 --reason-code spec-conflict --scope F07/s-canvas --file <內文檔>`， 內文用「情況：兩處矛盾並列」，把上面兩段原文逐字（『』包住）並列，推論另起一行，問題與選項各一行。不要改任何 spec／ui 文件本體。 |

## 第 2 輪 Review 退回

| ID | 回合 | 狀態 | 延續 | 描述 |
|---|---|---|---|---|
| D-05 | 2 | done | — | `uc-set-item-anchor` 與 `uc-remove-items` 有實作但沒有任何前端測試，而這兩者都是 `ui-canvas-layout.md`「驗收條件」逐字列出的項目。 `.dev/F07-canvas-layout/ui-canvas-layout.md`「驗收條件」逐字： 『- 移動、調整大小、設定能力、設定錨定方式、調整層序後，對應元件立即顯示更新後的內容，並分別觸發 `uc-move-item`／`uc-resize-item`／`uc-set-item-capabilities`／`uc-set-item-anchor`／`uc-reorder-item`』 『- 觸發批次移除前顯示確認；批次移動、批次移除操作後，所選元件皆依結果更新或移除，並分別觸發 `uc-move-items`／`uc-remove-items`』 實作現況（我實際查核）： - `kanban-frontend/src/canvas/CanvasStage.tsx` `handleToggleAnchor()` 呼叫 `canvasApi.setItemAnchor()`，`handleConfirmRemove()` 在 `removeTarget.length > 1` 時呼叫 `canvasApi.removeItems()`，兩條路徑都存在。 - 但 `kanban-frontend/src/pages/BoardCanvasPage.test.tsx` 的 14 個測試（`grep -n "it("`）沒有任何一則涵蓋這兩個操作：`grep -n "anchor\｜removeItems\｜批次移除\｜固定於畫面" src/pages/BoardCanvasPage.test.tsx` 只命中 fixture 資料裡的 `anchor: 'canvas'` 欄位，沒有命中任何斷言。 同一批 uc 的其餘九個（`uc-init-canvas`／`uc-place-item`／`uc-remove-item`／`uc-move-item`／`uc-resize-item`／`uc-set-item-capabilities`／`uc-reorder-item`／`uc-move-items`／`uc-set-viewport`）都有對應測試，只有這兩個沒有，沒有理由單獨略過；而且 `uc-set-item-anchor` 的送出值還經過 `canvas/geometry.ts` 的 `convertBoxForAnchor()` 換算（decision-log 第 1 輪判斷第 5 點自行決定的換算邏輯），完全沒有測試把關。 怎樣才算修好： 1. 新增一則測試：選取某個 `anchor` 為 canvas 的元件，按浮動工具列「固定於畫面」，斷言呼叫 `PATCH .../anchor`（或對應的 `canvasApi.setItemAnchor` mock）帶到的 `anchor` 為 `screen`，且 `x`／`y`／`width`／`height` 是依當時 `viewport`（建議用 `zoom !== 1` 的 viewport，才驗得到換算）換算後的值；並斷言回應套用後畫面上該元件的顯示位置。 2. 新增一則測試：以 shift 多選兩個元件後按批次「移除」，斷言先出現確認對話框（`ui-canvas-layout.md` 操作表「批次移除元件…需確認？＝是」），確認後呼叫 `canvasApi.removeItems` 且帶到兩個 item id，畫面上兩個元件都消失。 3. 至少其中一則補上失敗路徑斷言（例如批次移除回 409 時顯示後端訊息、元件仍在畫面上），對應 `uc-remove-items` p1／p2「所選元件皆不變，顯示訊息」。 不需要改動既有實作行為；如果補測試時發現實作有 bug，一併修掉並在 decision-log 說明。 |
