# T-03-be-card 修正任務（D-xx）

> 由 `loopctl fix add`／`fix done` 維護，不要直接編輯。

## 第 1 輪 Review 退回

| ID | 回合 | 狀態 | 延續 | 描述 |
|---|---|---|---|---|
| D-01 | 1 | done | — | 【uc-delete-swimlane／uc-delete-stage 協調流程非原子，失敗時會留下半套資料】 檔案：kanban-spring/src/main/java/io/progden/kanban/spring/application/BoardApplicationService.java 的 `removeSwimlane(..., confirmed)`／`removeStage(..., destinationStageId)`。整個 kanban-spring 沒有任何 `@Transactional`（`grep -rn Transactional kanban-spring/src/main` 無結果），而且兩個方法都是「先逐張 `cardRepository.save` 改卡片，最後才呼叫 `board.removeSwimlane`／`board.removeStage` 做 `ensureSwimlaneRemovable`／`ensureStageRemovable` 檢查」。每次 `save` 各自提交，後面的檢查一丟例外，前面改掉的卡片不會回滾。 具體會出錯的情境（依程式碼推得）： 1. 看板只剩 1 個 Swimlane 且內有卡片，呼叫 `DELETE .../swimlanes/{id}?confirmed=true`：卡片先被軟刪除，接著 `MINIMUM_SWIMLANE` 拒絕 → Swimlane 還在、卡片卻已經不見。spec `uc-delete-swimlane` fail p1 寫的是『拒絕，該 `swimlane` 不被刪除』，拒絕時卡片不應該被動到。 2. `swimlaneId` 不屬於該 board（或 `stageId` 不屬於該 board）：`findActiveBySwimlaneId` 只用 swimlaneId 查，會先刪掉／搬走別的看板的卡片，然後才丟 NOT_FOUND。 3. `destinationStageId` 等於來源 Stage、不存在、或屬於別的看板：前兩者會寫入 StageTransition 後被拒絕；後者會把卡片搬到不存在的 Stage，然後來源 Stage 因卡片數歸零而被成功刪除，留下孤兒卡片（違反 `uc-delete-stage` post『`card.stage` 更新為使用者選擇的目的 `stage`』——目的必須是這個 board 的 `stage`）。 修好的標準： - 先完成 board 端的可刪除檢查（最少數量、swimlane/stage 屬於該 board、目的 Stage 存在於同一 board 且不等於來源），檢查通過才動卡片；並且讓「卡片刪除／轉移＋board 刪除 Swimlane/Stage」在同一個交易裡（例如 application service 方法加 `@Transactional`），任一步失敗全部回滾。 - 目的 Stage 不合法時要回應什麼錯誤、訊息為何，spec 沒定義——照 ADR-001 的 fail→HTTP 慣例自行決定並記 decision-log；若認為需要業務確認，另開 OQ。 - 補測試（Cucumber 以外的 Spring 整合測試即可，不要改 feature 檔）至少覆蓋情境 1 與情境 3（目的 Stage 不屬於該 board），斷言拒絕後卡片的 `deleted`／`stage`／StageTransition 數量都不變。 |
| D-02 | 1 | done | — | 【Cucumber 斷言沒有驗到 spec 要求的內容】 1. `CardSteps.thenStageTransitionRecorded`（Scenario「卡片跨 Stage 移動（更新工作狀態）」，步驟『卡片的狀態異動應該被記錄，包含操作人、異動時間與異動前後的 Stage』）只斷言 `toStageId`，沒有斷言 `fromStageId` 等於移動前的 Stage「待辦」。`Card.moveToStage` 若把 from 記錯（例如記成目的 Stage），這個 Scenario 仍會綠燈。修好的標準：在 When 步驟記下移動前的 stageId，Then 斷言 `fromStageId` 等於它、`toStageId` 等於「進行中」。 2. `BoardSteps.createTestCard`（供「Swimlane 內有 N 張卡片」「Stage 內包含 N 張卡片」前置步驟使用）沒有檢查回應狀態；建卡失敗時前置資料會少卡，後續刪除情境可能在「0 張卡片」下假綠燈。修好的標準：斷言 201，失敗時帶出回應內容。 |
| D-03 | 1 | done | — | 【規格沒定義的失敗路徑，Dev 自行決定卻沒有開 OQ】 依規則書第 5 節『spec 的 `pre`／`post`／Scenario 沒講清楚該怎麼實作…＝高風險，寫進交接摘要「待確認事項」＋ OQ』。交接摘要把以下兩項列為「低風險技術決定，未開 OQ」，但它們都是在 spec 沒有 fail 定義的 usecase 上新增（或刻意不做）拒絕路徑，屬於高風險，需要用 `loopctl oq add --level 高 --blocking no` 開成 OQ（引文要到 spec／ui 逐字引用）： 1. 決策紀錄第 4 點：`uc-add-comment` 的 fail 為 `{}`，ui 的 `s-card-detail` 操作表也寫『不適用（`uc-add-comment` 無 fail 定義）』，但程式碼依欄位表 `comment.content` 的『非空』拒絕空白留言並回 400 `EMPTY_COMMENT_CONTENT`。欄位限制與 uc 的 fail／ui 呈現之間是規格缺口，要開 OQ 問是否補 fail 分支（會牽動 ui 的「失敗時」欄）。 2. 決策紀錄第 5 點：`uc-add-card` 不驗證 `swimlaneId`／`stageId` 是否存在且屬於該 board。名詞表寫 `card.swimlane`／`card.stage` 為『ref swimlane』『ref stage』、『建立時必填』，但 uc 只有 fail p1（標題留空）；目前可以用不存在的 UUID 建出孤兒卡片（`uc-move-card-swimlane`／`uc-move-card-stage` 的目的也一樣沒驗證）。要開 OQ 問是否要補對應的 fail；實作面可先依你的推論處理（例如驗證並拒絕），並在 OQ 內註明目前的做法。 修好的標準：`loopctl show` 列得出這兩則（或合併為一則但兩點都有逐字引文的）OQ，交接摘要的「待確認事項」指向它們。 |
