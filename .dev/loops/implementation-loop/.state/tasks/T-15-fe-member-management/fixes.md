# T-15-fe-member-management 修正任務（D-xx）

> 由 `loopctl fix add`／`fix done` 維護，不要直接編輯。

## 第 1 輪 Review 退回

| ID | 回合 | 狀態 | 延續 | 描述 |
|---|---|---|---|---|
| D-01 | 1 | done | — | 「移除成員」一律要先顯示確認，現況只在「仍是卡片負責人」時才確認。 依據（逐字引用 `.dev/F02-user-membership/ui-user-membership.md` `s-member-management`「操作」表兩列的「需確認？」欄）： - 移除成員：『是（附掛於清單列上；若該成員仍為 `card` 負責人，依 pre p2 需先顯示確認訊息並告知張數）』 - 變更成員角色：『是（附掛於清單列上；spec 未定義將 Owner 降級為 Member 的操作，無法復原）』 `.dev/conventions/spec-convention.md` 對這個欄位的定位：『確認／取消等互動改由 `ui-<模組>.md` 操作表的「需確認？」欄承接』。 問題：兩列的欄位值同樣是「是」，括號內只是補充「附掛於清單列上」的呈現方式與各自不可復原／需告知張數的理由。現行實作對「變更成員角色」做了無條件確認（`MemberManagementDialog.tsx` 的 `roleChangeTarget` 確認卡），對「移除成員」卻只在後端回 409 且訊息符合 `CARD_ASSIGNEE_CONFIRMATION_PATTERN` 時才顯示確認卡（`handleRemoveClick`），同一個欄位值被讀成兩種行為。`.dev/ui-prototype/MemberManagement.dc.html` 的設計註記也寫『設為 Owner → uc-change-member-role 移除 → uc-remove-member』『兩者都需確認：設為 Owner 不可逆（spec 未定義將 Owner 降級為 Member 的操作）』（設計稿只是佐證，以 ui 檔為準）。 怎樣才算修好： 1. `kanban-frontend/src/canvas/members/MemberManagementDialog.tsx`：點擊任一列的「移除」先顯示附掛於該列的確認卡（比照既有 `member-management-dialog__confirm`，取消／移除兩個按鈕），不先打 API。 2. 使用者在確認卡按「移除」才呼叫 `removeMember(..., confirmed=false)`；後端回 409 且訊息符合卡片負責人樣式時，把確認卡的訊息換成後端回傳的張數訊息並要求再次確認，確認後以 `confirmed=true` 重打（`uc-remove-member` pre p2「若目標成員仍是 `card` 負責人，使用者已確認移除」的既有流程保留）。 3. 其他失敗（例如 `uc-remove-member` fail p1「看板至少需要保留一位 Owner」）維持現況：顯示訊息、該成員留在清單中。 4. `MemberManagementDialog.test.tsx` 既有測試「移除非唯一 Owner 或 Member 後，該成員自清單移除，不需額外確認」的標題與斷言同步改成「先顯示確認、確認後才移除」，並確認驗收條件『移除非唯一 Owner 或 Member 後，觸發 `uc-remove-member`，該成員自清單移除』與『移除仍是卡片負責人的成員時，先顯示確認訊息並告知卡片張數，確認後才觸發 `uc-remove-member`』兩條都仍有對應測試。 5. `OQ-T-15-fe-member-management-02` 的問題 (1) 依本條處理（ui 操作表該欄已寫「是」，不是規格未定義），請在該 OQ 底下或 `decision-log.md` 註明；問題 (2)（`r-board-member` 是否需要「嘗試移除成員」的拒絕情境）維持待處理，是真的規格未定義。 |
| D-02 | 1 | done | — | 角色為 Viewer 的成員列不應該顯示「設為 Owner」。 依據（逐字引用 `.dev/F02-user-membership/spec-user-membership.md` `uc-change-member-role`）： ``` pre: p1: "變更者是該 `board` 的 Owner" p2: "目標使用者是該 `board` 的 Member" ``` `.dev/F02-user-membership/ui-user-membership.md` `s-member-management`「資料」表「變更後角色」一列：『依 `uc-change-member-role` pre p2：目標須為該 `board` 的 Member；本用例僅支援 Member 升級為 Owner，spec 未定義任何操作可將 Owner 降級為 Member』。 問題：`kanban-frontend/src/canvas/members/MemberManagementDialog.tsx` 的條件是 `member.role !== 'OWNER'` 才顯示「設為 Owner」。`board-membership.role` 依 spec 欄位表為 `enum(Owner, Member, Viewer)`，所以角色為 Viewer 的列也會出現這個按鈕，點下去會對一個不滿足 pre p2 的目標觸發 `uc-change-member-role`。後端 `BoardMembershipApplicationService.changeMemberRole`（T-04）沒有檢查 pre p2，會直接把 Viewer 改成 Owner。 怎樣才算修好： 1. 把顯示條件改成只有 `member.role === 'MEMBER'` 時才顯示「設為 Owner」。 2. 補一個測試：清單中角色為 VIEWER 的成員列沒有「設為 Owner」按鈕。 3. 後端缺 pre p2 檢查是 T-04 的範圍，不在本任務改；已由本輪 `OQ-T-15-fe-member-management-03` 記錄本畫面該不該顯示 Viewer 列的文件矛盾，這裡只處理前端不提供違反 pre p2 的入口。 |
