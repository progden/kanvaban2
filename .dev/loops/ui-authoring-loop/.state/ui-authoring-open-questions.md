# UI 撰寫待決事項（OQ）

撰寫過程中遇到的**高風險決策**（見 [`ui-authoring-prompt.md`](../prompts/ui-authoring-prompt.md)「自主決策分級」）。loop 不停下等人：遇到 spec 沒寫清楚、推不出來的資訊，選「最小驚訝、與 spec 現有內容一致」的處理方式，標 `⚠️` 留在該畫面「待確認事項」，並在下表末尾追加一列；對應畫面的狀態維持「討論中」，不標「已定案」。人工事後檢視，不同意的再回頭修正對應 ui 檔或（若是 spec 本身缺東西）回饋給 SA 走 CR。

規則：

- **只能在表格末尾追加列**，不可修改或刪除既有列，表格之後不可再加其他內容。
- 編號 `OQ-01` 起流水號；「採用」寫實際採用的處理方式，「依據」寫依據哪個 Use Case／欄位／Scenario（模組、Feature、uc 或欄位 ID），兩欄都不可空白。
- 狀態：`自動決議`（loop 已照採用的選項做，畫面留在「討論中」）／`blocked`（環境限制或「覆蓋 spec」，對應任務已標 `blocked`）。
- **「情況」欄禁止只寫抽象結論**，必須是下列四種寫法之一，並在欄位開頭標明種類：
  1. 【引用原文】直接複製 spec 對應段落的原句（Scenario 步驟、欄位表「限制」欄、usecase 區塊值），用『』包住逐字原文，附出處（`uc-`／`entity.attr`／Feature 名稱）。
  2. 【矛盾】並列兩處互相衝突的 spec 原文，各自用『』包住並附出處；「選項」欄固定給三個、不得增減：**保留 A**（沿用第一處原文的結論）／**保留 B**（沿用第二處原文的結論）／**缺區分條件**（兩處都不採用，維持討論中，待補充能區分適用情境的規則）。
  3. 【推論】先用『』引用推論所本的原始 spec 句子＋出處，再用一句話寫從它推出的結論；不得只寫結論、省略所本原文。
  4. 【覆蓋】這一輪的答案跟 spec 已寫清楚的內容不一致，等於要動到定稿文字時用這種——固定格式「這會覆蓋 `spec-<模組>.md`「<段落／`uc-`／Scenario 名稱>」原文：『逐字引用原文』→ 若採用新答案，需改為：『新文字』」。用【覆蓋】的列，「採用」欄一律填「暫不覆蓋，維持 spec 原文；待 CR 核准後再改」，「狀態」欄一律是 `blocked`，對應任務同步標 `blocked`——**不可以自動決議掉一個覆蓋 spec 的答案**。
  目的是讓人工事後一眼分辨這是 spec 寫的、spec 自相矛盾、loop 推出來的，還是要靠 CR 才能定案的覆蓋，不得用「應該」「我認為」「比較好」等說服性字眼取代引用。
  **凡是『』包住的文字，都必須與對應 `spec-<模組>.md` 逐字相符**（【覆蓋】只要求「原文：」之後、「→」之前那段逐字相符，「新文」不受此限）；收尾前執行 `python3 .dev/loops/ui-authoring-loop/scripts/verify-quotes.py` 驗證，有任何一筆比對不到就視為本輪未完成，要嘛修正引用使其逐字相符，要嘛承認引用錯誤重寫該列。
- **每一列「情況」欄開頭固定加 `[Level: <模組>/<uc 或 Screen ID>]` 標記**（有具體 Scenario 名稱時可再加一層 `/<Scenario 名稱>`），例如 `[Level: user-membership/uc-invite-member] 【引用原文】...`，讓人工不用回頭查任務清單就知道這筆問題屬於哪個顆粒度。
- **新增一列前先查重**：掃過本表既有列與各 `ui-<模組>.md`「待確認事項」，同一個 `uc-`／entity／Screen 已經有紀錄時不要重開一筆；若這輪的結論要推翻先前答案，「情況」欄要明講「推翻 OQ-xx」並說明差異來源，不能悄悄新增一筆看似獨立的問題。
- **「選項」欄若牽涉挑選既有類別**（角色、Use Case、Screen 等）：把該類別在 spec／本檔目前的**完整清單**列出（例如問角色就把角色表全部角色列出，不是只列候選的兩個），讓人工看得到還有沒有其他既有選項可用；清單裡真的沒有合適項目時，才能額外加一個「新增」選項並註明是新提案，不是既有項目。

| 編號 | 日期 | 模組 | 情況 | 選項 | 採用 | 依據 | 狀態 |
|---|---|---|---|---|---|---|---|
| OQ-01 | 2026-09-17 | kanban-basic | [Level: kanban-basic/s-swimlane-list] 【推論】`spec-kanban-basic.md`「Feature: Swimlane 管理」Background 僅寫『And 我已開啟一個名為 "產品開發看板" 的看板』，未描述如何從看板畫面前往 Swimlane 管理；推論 `s-swimlane-list` 的進入路徑需待 `s-board` 定案時一併確認。 | 1. 模組入口（獨立入口，不經看板） 2. 從 `s-board` 的某個操作進入 3. 併入 `s-board` 內嵌顯示，`s-swimlane-list` 不需獨立成畫面 | 暫定為模組入口，維持八段格式完整；待 `s-board` 定案時回頭確認或更正 | Feature: Swimlane 管理 Background | 自動決議 |
| OQ-02 | 2026-09-17 | kanban-basic | [Level: kanban-basic/s-stage-list] 【推論】`spec-kanban-basic.md`「Feature: Stage（階段）管理」Background 僅寫『And 我已開啟一個名為 "產品開發看板" 的看板』『And 看板目前的 Stage 依序為 "待辦"、"進行中"、"完成"』，未描述如何從看板畫面前往 Stage 管理；與 OQ-01 同類問題，推論 `s-stage-list` 的進入路徑同樣需待 `s-board` 定案時一併確認。 | 1. 模組入口（獨立入口，不經看板） 2. 從 `s-board` 的某個操作進入 3. 併入 `s-board` 內嵌顯示，`s-stage-list` 不需獨立成畫面 | 暫定為模組入口，維持八段格式完整；待 `s-board` 定案時回頭確認或更正 | Feature: Stage（階段）管理 Background | 自動決議 |
| OQ-03 | 2026-09-17 | kanban-basic | [Level: kanban-basic/uc-delete-stage] 【推論】`spec-kanban-basic.md` `uc-delete-stage` post 僅寫『若該 `stage` 內有 `card`，`card.stage` 更新為使用者選擇的目的 `stage`』，未寫目的 `stage` 是否可以是欲刪除的 `stage` 本身；推論 `s-stage-delete-dialog` 的目的 Stage 選擇欄位是否需排除該 Stage 本身待確認。 | 1. 排除欲刪除的 Stage 本身（清單只列其他 Stage） 2. 不排除，允許選擇同一個 Stage（形同不轉移） 3. 沿用 spec 原文，維持討論中不預設 | 暫定排除欲刪除的 Stage 本身（`s-stage-delete-dialog` 資料段驗證欄標 ⚠️），維持該畫面「討論中」；待補充規則後確認 | uc-delete-stage post | 自動決議 |
| OQ-04 | 2026-09-17 | kanban-basic | [Level: kanban-basic/s-board] 【引用原文】`spec-kanban-basic.md` 各 Feature 的 Background 統一寫『And 我已開啟一個名為 "產品開發看板" 的看板』，未描述如何「開啟」看板；開啟看板屬跨模組行為，定義於 `.dev/F02-user-membership/spec-user-membership.md`，對應畫面（任務清單 T2.01 規劃的 `s-board-list`）尚未撰寫。 | 1. 待 F02 `s-board-list` 定案後回填「從哪裡進來」 2. 現在先行假設 `s-board-list` 存在並直接以反引號引用（風險：F02 尚未定案，ID 或流程可能與後續設計不符） 3. 「從哪裡進來」留白（不符合 `ui-convention.md` 八段格式要求） | 選項 1：`s-board`「從哪裡進來」標 ⚠️ 待確認，說明跨模組依賴 F02 選擇／開啟看板，暫不引用尚未定案的 `s-board-list` ID；維持該畫面「討論中」 | Feature 各 Background 統一語句；`ui-authoring-tasks.md` T2.01 規劃的 `s-board-list` | 自動決議 |
| OQ-05 | 2026-09-17 | kanban-basic | [Level: kanban-basic/s-board] 【推論】`.dev/F02-user-membership/spec-user-membership.md` `uc-assign-card-owner-by-drag` Scenario『When 我將 "雅婷" 的頭像拖曳到卡片 "設計登入頁面" 上』顯示拖曳來源是「成員頭像」，但未描述該頭像清單顯示在看板畫面的哪個位置（是否為固定側欄、每張卡片內建等）；推論 `s-board` 需要呈現看板成員清單供拖曳，但呈現位置無 spec 依據。 | 1. 固定側欄／面板列出所有看板成員頭像 2. 依附在其他既有畫面（例如 F02 的成員管理畫面）而非 `s-board` 本身 3. 沿用 spec 原文，維持討論中不預設 | 暫定 `s-board` 資料段列出「看板成員清單」欄位並標 ⚠️，不預設呈現位置；維持該畫面「討論中」 | uc-assign-card-owner-by-drag Scenario：拖曳成員頭像到卡片上，追加該成員為負責人 | 自動決議 |
| OQ-06 | 2026-09-17 | user-membership | [Level: kanban-basic/s-board（跨模組，見 user-membership「Feature: 卡片負責人指派」）] 【矛盾】「Feature: 卡片負責人指派」的 usecase 區塊（`uc-set-card-assignees`／`uc-list-card-assignee-candidates`／`uc-view-card-assignees`／`uc-list-cards-by-assignee`／`uc-assign-card-owner-by-drag`）皆寫『roles: [r-user]』，但「角色定義」表僅列出『r-system-user』『r-board-owner』『r-board-member』三個 ID，沒有 `r-user`（`r-user` 只在另一模組「看板基本使用案例」規格的角色定義中出現）。 | 保留A：沿用 usecase 區塊的 `r-user`（視為刻意沿用另一模組的角色 ID） 保留B：沿用角色定義表，`roles` 應為 `r-system-user`／`r-board-owner`／`r-board-member` 之一（需回饋哪一個） 缺區分條件：兩處都不採用，維持討論中，待補充能區分適用情境的規則 | `s-board`「操作」「角色與權限」表暫依 usecase 區塊填 `r-user`，並於「待確認事項」標 ⚠️ 回饋此矛盾；維持該畫面「討論中」 | uc-assign-card-owner-by-drag 等 5 個 usecase 的 roles 欄位；user-membership「角色定義」表 | 自動決議 |
| OQ-07 | 2026-09-17 | kanban-basic | [Level: kanban-basic/s-swimlane-list、s-stage-list] 【推論】推翻 OQ-01、推翻 OQ-02：`spec-kanban-basic.md`「Feature: Swimlane 管理」與「Feature: Stage（階段）管理」的 Background 皆只寫『And 我已開啟一個名為 "產品開發看板" 的看板』，本身仍未描述如何從看板前往 Swimlane／Stage 管理；T1.06 定案 `s-board` 時在其操作表新增「管理 Swimlane」「管理 Stage」兩列（觸發 `—`），故可推論 `s-swimlane-list`／`s-stage-list` 的進入路徑是 `s-board` 的這兩個操作，而非 OQ-01／OQ-02 原先暫定的「模組入口」。但 `s-board` 本身「從哪裡進來」仍為討論中（見 OQ-04／OQ-05／OQ-06），這個推論只解掉「Swimlane／Stage 管理從哪裡進來」，不代表 `s-board` 已定案。 | 1. 模組入口（獨立入口，不經看板，即 OQ-01／OQ-02 原結論） 2. 從 `s-board` 的「管理 Swimlane」／「管理 Stage」操作進入（本列採用） 3. 併入 `s-board` 內嵌顯示，不需獨立成畫面 | 從 `s-board` 的「管理 Swimlane」／「管理 Stage」操作進入；`s-swimlane-list`／`s-stage-list`「從哪裡進來」引用本列，但不得宣稱 `s-board` 本身已定案 | Feature: Swimlane 管理 Background；Feature: Stage（階段）管理 Background；`s-board`「操作」表「管理 Swimlane」「管理 Stage」列（T1.06） | 自動決議 |
| OQ-08 | 2026-09-17 | kanban-basic | [Level: kanban-basic/s-card-detail] 【引用原文】`spec-kanban-basic.md` 變更紀錄 CR-002 寫『「編輯卡片詳細內容」移除負責人欄位，負責人改為多選、參照看板成員，改由 F02「卡片負責人指派」情境處理』，故 F01 `card` 欄位表無負責人欄位，`s-card-detail` 顯示負責人與指派入口需待 F02 `s-card-assignee-picker`（任務清單 T2.08／T2.11 規劃）定案後回填。 | 1. 待 F02 `s-card-assignee-picker` 定案後回填（本列採用，亦為 T2.11 收尾任務規劃） 2. 現在先行假設 `card.assignees` 與 `s-card-assignee-picker` 已定案並直接引用（風險：F02 尚未定案，ID 或流程可能與後續設計不符） 3. 負責人欄位不列入資料表（不符合 `ui-convention.md` 資料表需列出畫面顯示欄位的要求） | `s-card-detail`「資料」段負責人欄位與「待確認事項」暫標 ⚠️ 待 F02 `s-card-assignee-picker` 定案後回填；維持該畫面「討論中」 | 變更紀錄 CR-002；`ui-authoring-tasks.md` T1.08／T2.08／T2.11 | 自動決議 |
| OQ-09 | 2026-09-17 | kanban-basic | [Level: kanban-basic/uc-add-comment] 【引用原文】`spec-kanban-basic.md` `uc-add-comment` post 寫『新增的留言加入 `card` 的留言列表中』『留言連同留言者與留言時間一併保存於 `card`』，但「名詞定義」的實體表與欄位表均未定義「留言」實體或任何留言相關欄位，`s-card-detail` 資料表列出留言相關欄位時找不到可引用的 Attribute ID。 | 1. 資料表以文字描述留言相關欄位（留言內容、留言者、留言時間），不掛反引號 ID，並標 ⚠️ 回饋後端 Spec 補上「留言」實體與欄位定義（本列採用） 2. 自行在 ui 檔定義一個新的 `comment` 實體或欄位 ID 供反引號引用（違反鐵則 1「不定義新概念」，不採用） 3. 資料表不列留言相關欄位，僅在待確認事項提及（不符合 `ui-convention.md` 要求資料表列出畫面顯示欄位） | 資料表以文字描述留言相關欄位，不掛反引號 ID，「待確認事項」標 ⚠️ 回饋後端 Spec 補上「留言」實體與欄位定義；維持該畫面「討論中」 | `uc-add-comment` post；名詞定義 實體表／欄位表（無「留言」相關項目） | 自動決議 |
| OQ-10 | 2026-09-17 | user-membership | [Level: user-membership/uc-assign-card-owner-by-drag（跨模組，影響看板主畫面 s-board）] 【推論】`spec-user-membership.md` `uc-assign-card-owner-by-drag` post 寫『若該成員原本不在 `card.assignees` 中，追加為負責人，`card.assignees` 包含追加後的完整清單』，確認 `card.assignees`／`uc-assign-card-owner-by-drag`／`board-membership`／`r-board-member`／`r-board-owner`／`r-system-user` 六個 ID 皆已在 `spec-user-membership.md` 定義，`s-board` 操作表對它們的跨模組引用（T1.06 定案）內容正確；但單獨對 `ui-kanban-basic.md` 這一份檔案跑 `ui-check` 時，工具只會自動載入 F01 自己同目錄同模組的 spec 檔（見 `scripts/speccheck/runner.py` 的「specs_for_designs」：ui 檔只對應同目錄同模組的 spec，不含其他模組），不會載入 `spec-user-membership.md`，導致這 12 筆合法跨模組引用被 `REF-07` 誤判為未定義；推論 T1.10 那條只針對單一檔案（不含 `--spec`）的驗收條件，在 `s-board` 保留這些跨模組引用的前提下無法機械達成，屬工具對單模組檔案的檢查範圍限制，非畫面內容缺陷（改成同時帶 `--spec spec-user-membership.md` 執行，這 12 筆全部消失，僅剩 F02 自身「uc-set-card-assignees」等 8 筆 `DS-06` warn，與 F01 內容無關）。 | 1. 判定為環境限制：OQ 記錄＋任務標 `blocked`，等待人工調整驗收條件或 `ui-check` 工具本身（本列採用） 2. 刪除或改寫 `s-board` 對這 6 個 F02 ID 的反引號引用，改成不掛 ID 的純文字敘述，讓單一檔案 `ui-check` 通過（違反 `ui-authoring-prompt.md`「跨模組可以互相引用……ID 存在即可，不因為跨模組就要重新定義」，且等同為了讓檢查通過而刪減內容，不採用） 3. 提前建立 `ui-user-membership.md` 讓檢查間接帶入 F02 spec（單一檔案目標仍不會納入其他模組 spec，且會搶跑 T2.01 的任務範圍，不採用） | 判定為環境限制：OQ 記錄＋ T1.10 標 `blocked`，等待人工決定（調整驗收條件為含 `--spec` 版本、或改成跨模組收尾階段才檢查、或調整 `ui-check` 工具本身的載入邏輯）後才能改回 `todo` | `spec-user-membership.md` uc-assign-card-owner-by-drag post；`scripts/speccheck/runner.py` 的「specs_for_designs」行為 | blocked |
| OQ-11 | 2026-09-17 | user-membership | [Level: user-membership/s-signup] 【引用原文】`spec-user-membership.md`「Feature: 建立使用者帳號」僅寫『我想要建立一個帳號，密碼規則越單純越好』『以便快速取得系統存取權而不被過度嚴格的密碼規則卡住』，沒有 Background，也未描述使用者從何處抵達建立帳號的畫面。 | 1. 模組入口（未登入使用者直接進入，本列採用） 2. 從 `s-login` 的連結進入（需 `s-login` 定案時同步確認） 3. 沿用 spec 原文，維持討論中不預設 | 暫定為模組入口，`s-signup`「從哪裡進來」標 ⚠️；維持該畫面「討論中」；待 `s-login` 定案時回頭確認或更正 | Feature: 建立使用者帳號 說明句 | 自動決議 |
| OQ-12 | 2026-09-17 | user-membership | [Level: user-membership/uc-create-user] 【推論】`uc-create-user` post 僅寫『新的 `user` 建立成功，`user.username`、`user.display-name`、`user.password` 依輸入值設定』，未描述建立成功後畫面導向；Scenario「建立帳號時密碼可以留白」另外寫『Then 該帳號應該建立成功』『And 我應該能用空白密碼登入這個帳號』，登入被寫成獨立的 Then 步驟（對應另一個 `uc-login`），推論建立成功後不是自動登入，而是導向登入相關畫面，但確切目的地 spec 未寫明。 | 1. 導向 `s-login`，提示登入（本列採用） 2. 停留本畫面顯示成功訊息 3. 沿用 spec 原文，維持討論中不預設 | 暫定完成後導向 `s-login`，`s-signup`「完成後去哪裡」標 ⚠️；維持該畫面「討論中」 | uc-create-user post；Scenario 建立帳號時密碼可以留白 | 自動決議 |
| OQ-13 | 2026-09-17 | user-membership | [Level: user-membership/uc-login] 【引用原文】`uc-login` post 僅寫『登入成功，TopBar 顯示該 `user` 的帳號名稱』，未描述登入成功後導向哪個畫面。 | 1. 導向 `s-board-list`（本模組規劃的「我的 Board 列表」畫面，任務清單 T2.02 尚未定案，本列採用） 2. 停留 `s-login` 本畫面，僅更新 TopBar 顯示 3. 沿用 spec 原文，維持討論中不預設 | 暫定導向 `s-board-list`，`s-login`「完成後去哪裡」標 ⚠️；維持該畫面「討論中」 | uc-login post | 自動決議 |
| OQ-14 | 2026-09-17 | user-membership | [Level: user-membership/uc-view-board-list] 【引用原文】`spec-user-membership.md`「Feature: Board 存取權限」Scenario「Board 列表只顯示我有權限的 Board」寫『Given 我是 Board "產品開發看板" 的 Owner』『Then 列表應該顯示 "產品開發看板" 與 "行銷活動排程"』，顯示每個 Board 有一個可識別的名稱，但「名詞定義」欄位表只定義了 `board.created-by`，沒有 `board.name` 或類似欄位，`s-board-list` 資料表列出 Board 名稱欄位時找不到可引用的 Attribute ID（與 OQ-09「留言」欄位缺 ID 同類問題）。 | 1. 資料表以文字描述「Board 名稱」欄位，不掛反引號 ID，並標 ⚠️ 回饋後端 Spec 補上 `board.name` 欄位定義（本列採用，同 OQ-09 處理方式） 2. 自行在 ui 檔定義一個新的 `board.name` 欄位 ID 供反引號引用（違反鐵則 1「不定義新概念」，不採用） 3. 資料表不列 Board 名稱欄位，僅在待確認事項提及（不符合 `ui-convention.md` 要求資料表列出畫面顯示欄位） | 資料表以文字描述「Board 名稱」欄位，不掛反引號 ID，「待確認事項」標 ⚠️ 回饋後端 Spec 補上 `board.name` 欄位定義；維持該畫面「討論中」 | Feature: Board 存取權限 Scenario：Board 列表只顯示我有權限的 Board；名詞定義 欄位表（無 `board.name` 相關項目） | 自動決議 |
| OQ-15 | 2026-09-17 | user-membership | [Level: user-membership/uc-reject-board-access-by-nonmember] 【引用原文】`uc-reject-board-access-by-nonmember` post 僅寫『系統顯示錯誤訊息「你沒有權限存取這個看板」，操作者仍無法存取該 `board`』，未描述顯示這則錯誤訊息時停留或導向哪個畫面。 | 1. 停留於 `s-board-list`（因為找不到其他更明確的目標畫面，本列採用） 2. 導向 F01 `s-board` 並疊加錯誤訊息 3. 沿用 spec 原文，維持討論中不預設 | 暫定停留於 `s-board-list`，該操作列「成功後」欄標 ⚠️；維持該畫面「討論中」 | uc-reject-board-access-by-nonmember post | 自動決議 |
| OQ-16 | 2026-09-17 | user-membership | [Level: user-membership/s-board-list（跨模組，引用 F01 `s-board`）] 【推論】`uc-view-board-list` post 寫『列表只顯示我是 Owner 或 Member 的 `board`，不顯示我沒有權限的 `board`』，推論使用者在列表中選到的每個 `board` 都必須能真正進入該 `board` 的畫面，故 T2.04 定案 `s-board-list`「進入與離開」「操作」「驗收條件」三處引用 F01 `ui-kanban-basic.md` 定義的 `s-board`（Screen ID，選擇列表中的 Board 進入該 Board），ID 本身已在 F01 定案，屬鐵則 6 允許的跨模組引用；但單獨對 `ui-user-membership.md` 這一份檔案跑 `ui-check`（`tools error-count`／`accept-check` 內部呼叫 `ui-authoring-tools.py` 的 `findings()`，其 `paths = [target_path] if target_path else existing`）只會把該檔案自己傳給 `ui-check` 當作要解析 Screen 定義的 design 檔，不會一併傳入 `ui-kanban-basic.md`，導致 `s-board` 這個合法跨模組 Screen 引用被 `REF-07` 誤判為未定義（4 筆）；改成同時把兩份 ui 檔一起傳給 `ui-check`（`./scripts/ui-check F01 的 ui-kanban-basic.md .dev/F02-user-membership/ui-user-membership.md --spec ".dev/F[0-9][0-9]-*/spec-*.md"`）後，這 4 筆 REF-07 全部消失，`s-board-list` 所在行號範圍內無其他 error／warn，確認是工具只載入單一 design 檔案的範圍限制，非畫面內容缺陷；與 OQ-10（`--spec` 只載入 target 自己模組的 spec）同一類問題，但 OQ-10 修的是 spec 檔清單、這筆是 design（ui 檔）清單，`findings()` 目前沒有對 design 路徑做同等的「全部既有 ui 檔」擴展。 | 1. 判定為環境限制：OQ 記錄＋任務標 `blocked`，等待人工調整 `ui-authoring-tools.py` 的 `findings()` 讓 `paths` 一律帶入全部既有 ui 檔（不只 target 自己），或調整驗收條件（本列採用） 2. 拿掉 `s-board-list` 對 `s-board` 的反引號引用，改成不掛 ID 的純文字敘述，讓單一檔案 `ui-check` 通過（違反 `ui-authoring-prompt.md`「跨模組可以互相引用……ID 存在即可，不因為跨模組就要重新定義」，且等同為了讓檢查通過而刪減內容，不採用，同 OQ-10 選項 2 的理由） 3. 提前把 `s-board` 從 F01 拿掉，`s-board-list` 完全不提是否可以進入某個 Board（不符合 `ui-convention.md` 八段格式「完成後去哪裡」需交代清楚的要求，不採用） | 判定為環境限制：OQ 記錄＋ T2.04 標 `blocked`，等待人工決定（調整 `findings()` 的 design 路徑邏輯、或調整驗收條件為含多檔案版本）後才能改回 `todo`（內容本身已完成：多檔案一起檢查時 `s-board-list` 所在行號範圍 0 error／0 warn） | `ui-authoring-tools.py` `findings()` 的 `paths = [target_path] ...` 邏輯；`s-board-list`「進入與離開」「操作」「驗收條件」對 `s-board` 的引用 | blocked |
| OQ-17 | 2026-09-17 | canvas-layout | [Level: canvas-layout/s-canvas（跨模組，推翻 OQ-01／OQ-02／OQ-07，影響 kanban-basic 的 `s-swimlane-list`／`s-stage-list`／`s-board`）] 【推論】人工於本輪明確決定產品方向：F07 是一個類似 Miro 的畫布，`spec-canvas-layout.md` 簡介寫『使用者開啟某個 Board 時，看到的就是該 Board 的 Canvas』，且『元件本身的內容與行為（例如圖表的資料、設定、互動）不屬於本模組，由元件所屬模組定義』；人工確認看板本體是固定顯示在 Canvas 中間的一個 `item`，新增 Swimlane／新增 Stage 是選中該 `item` 後，由 Canvas 左側選單觸發，不是 F01 `s-board` 操作表的「管理 Swimlane」／「管理 Stage」列（T1.06 定案內容）導向 `s-swimlane-list`／`s-stage-list` 兩個獨立畫面。推翻 OQ-01／OQ-02（原「模組入口」）與 OQ-07（原「從 `s-board` 的操作進入」）。 | 1. 維持 OQ-07 結論：從 `s-board` 操作列進入獨立畫面 2. 從 F07 `s-canvas` 選中看板 item 後的屬性／操作面板觸發，`s-swimlane-list`／`s-stage-list` 是否仍需獨立 Screen ID（列表或彈出清單）待撰寫 `s-canvas` 時一併判斷（本列採用） 3. 完全併入 `s-canvas`，不需要 `s-swimlane-list`／`s-stage-list` 這兩個 Screen ID | 採用選項 2：`uc-add-swimlane`／`uc-rename-swimlane`／`uc-reorder-swimlane`／`uc-delete-swimlane`／`uc-add-stage` 等 F01 uc 的觸發畫面改為 F07 `s-canvas`；F01 `s-board` 操作表移除「管理 Swimlane」「管理 Stage」兩列；`s-swimlane-list`／`s-stage-list` 的「進入與離開」改指向 `s-canvas`，是否仍需獨立 Screen ID 待 T7.02 撰寫 `s-canvas` 時判斷。已開 D-09 追蹤這個修正，依賴 T7.02（`s-canvas` 定案）完成後才能動手，理由：F07 `s-canvas` 現在還沒定案，沒有可以指向的目標 Screen ID | `spec-canvas-layout.md` 簡介；`ui-authoring-tasks.md` T1.06（`s-board` 已定案內容）、T7.02（`s-canvas` 待定案）；D-09 | blocked |
| OQ-18 | 2026-09-17 | canvas-layout | [Level: canvas-layout/s-board（跨模組，推翻 OQ-04，影響 kanban-basic 的 `s-board`）] 【引用原文】人工於本輪確認：`spec-canvas-layout.md` 簡介『使用者開啟某個 Board 時，看到的就是該 Board 的 Canvas』——`s-board` 不是使用者「被導向進入」的獨立畫面，而是以 F07 `item` 的形式，本來就顯示在 `s-canvas` 上；F07「待釐清」也明講『看板本體（F01 的看板主畫面）如何成為某個 Canvas 上的 `item`（`item.component` 填什麼值代表「看板本體」、預設的位置與大小為何），待整合時另開 CR』，代表確切機制尚未定案。推翻 OQ-04（原「待 F02 `s-board-list` 定案後回填」）。 | 1. 維持 OQ-04：`s-board`「從哪裡進來」待 F02 `s-board-list` 定案後回填、直接指向 `s-board-list` 2. `s-board`「從哪裡進來」改為「不適用——內容以 F07 `item` 形式顯示於 `s-canvas`，見 `spec-canvas-layout.md`」，使用者的實際導覽路徑是 `s-board-list` → `s-canvas`（本列採用，但確切機制待 F07「待釐清」項目定案，暫不視為完全解決） 3. 沿用 spec 原文，維持討論中不預設 | 採用選項 2：`s-board`「從哪裡進來」改寫為不適用／由 F07 `s-canvas` 承接，並註明機制仍待 F07「待釐清」定案；已開 D-09 一併追蹤，依賴 T7.02 | `spec-canvas-layout.md` 簡介與「待釐清」；`ui-authoring-tasks.md` T1.06、T7.02；D-09 | blocked |
| OQ-19 | 2026-09-17 | canvas-layout | [Level: canvas-layout/s-canvas（跨模組，推翻 OQ-05，影響 kanban-basic 的 `s-board`）] 【推論】人工於本輪確認：延續 OQ-17／OQ-18 的 F07 類 Miro 畫布方向，`spec-canvas-layout.md` 簡介『元件本身的內容與行為（例如圖表的資料、設定、互動）不屬於本模組，由元件所屬模組定義』，人工進一步確認「看板成員頭像清單」（`uc-assign-card-owner-by-drag` 拖曳來源，OQ-05 討論的呈現位置）也是 Canvas 上另一個獨立的 `item`，不是 F01 `s-board` 資料段的一部分。推翻 OQ-05（原「`s-board` 資料段列出看板成員清單欄位，不預設呈現位置」）。 | 1. 維持 OQ-05：`s-board` 資料段列出「看板成員清單」欄位，不預設呈現位置 2. 「看板成員清單」改為 F07 `s-canvas` 上另一個獨立 `item`（本列採用），`s-board` 資料段移除該欄位；`uc-assign-card-owner-by-drag` 的拖曳來源（成員頭像）與拖曳目標（`s-board` 上的卡片）分屬 F07 兩個不同 `item`，跨 `item` 拖曳的機制 `spec-canvas-layout.md` 目前未定義，須留待 F07 補充或視為前端層級的技術實作細節 3. 沿用 spec 原文，維持討論中不預設 | 採用選項 2：`s-board` 資料段移除「看板成員清單」列；「拖曳成員頭像到卡片追加負責人」操作列維持（拖放的目標仍是 `s-board` 上的卡片），但待確認事項改為說明成員頭像清單已改為獨立 `item`、跨 `item` 拖曳機制待 F07 補充；已開 D-09 一併追蹤，依賴 T7.02 | `spec-canvas-layout.md` 簡介；`ui-authoring-tasks.md` T1.06（`s-board` 已定案內容）、T7.02；D-09 | blocked |
| OQ-20 | 2026-09-17 | user-membership | [Level: user-membership/uc-set-card-assignees 等 5 個 usecase、uc-view-board-activity-log（推翻 OQ-06）] 【引用原文】人工於本輪直接修正 spec 解決矛盾（不是 loop 自動決議）：`spec-user-membership.md` 這 5 個卡片負責人 usecase 與 `uc-view-board-activity-log` 的 `roles` 已改為『roles: [r-board-member]』（前者）與『roles: [r-board-owner, r-board-member]』（後者），不再是造成矛盾的 `r-user`。推翻 OQ-06（原「`s-board` 暫依 usecase 區塊填 `r-user`，標 ⚠️ 回饋矛盾」）。 | （矛盾已由人工修正 spec 消除，不再需要三選項） | 已修正 `spec-user-membership.md`；`ui-kanban-basic.md` 的 `s-board` 角色與權限表新增 `r-board-member`（F02，跨模組）一列，移除 OQ-06 的 ⚠️ | `spec-user-membership.md` 變更紀錄 2026-09-17（角色欄位筆誤修正列）；`ui-kanban-basic.md` `s-board` 角色與權限表 | 自動決議 |
| OQ-21 | 2026-09-17 | kanban-basic | [Level: kanban-basic/uc-add-comment、s-card-detail（推翻 OQ-09）] 【引用原文】人工於本輪直接修正 spec 補上缺漏的實體（不是 loop 自動決議）：`spec-kanban-basic.md` 名詞定義已新增 `comment` 實體與欄位，`uc-add-comment` post 已改為『新的 `comment` 建立成功，`comment.content` 為輸入內容，`comment.author` 為留言者，`comment.created-at` 為留言的操作時間』，不再是找不到 Attribute ID 的狀態。推翻 OQ-09（原「資料表以文字描述留言相關欄位，不掛反引號 ID」）。 | （缺漏已由人工補上 spec，不再需要三選項） | 已修正 `spec-kanban-basic.md`；`ui-kanban-basic.md` 的 `s-card-detail` 資料表「留言列表」列改填 `comment.content`／`comment.author`／`comment.created-at`，移除 OQ-09 的 ⚠️ | `spec-kanban-basic.md` 變更紀錄 2026-09-17（`comment` 實體新增列）；`ui-kanban-basic.md` `s-card-detail` 資料表 | 自動決議 |
| OQ-22 | 2026-09-17 | user-membership | [Level: user-membership/s-member-management] 【推論】`spec-user-membership.md`「Feature: Board 建立與成員邀請」說明句寫『我想要建立 Board、邀請其他使用者加入，並可以指定多位共同擁有者』，僅描述邀請這個行為的意圖，Background 只有『Given 我已登入系統，帳號為 "user1"』，未描述使用者從哪個既有畫面前往執行邀請／變更角色／移除成員的畫面；推論入口應與該 `board` 本身相關，可能是 F01 `s-board` 的 Owner 專屬操作，但 `s-board` 操作表（T1.06 已定案內容）目前沒有這一項。 | 1. 模組入口（獨立入口，不經看板） 2. 從 F01 `s-board` 的 Owner 專屬操作進入（該操作尚未列在 `s-board` 操作表中，需回頭補上，本列採用） 3. 沿用 spec 原文，維持討論中不預設 | 暫定從 F01 `s-board` 的 Owner 專屬操作進入，`s-member-management`「進入與離開」標 ⚠️；維持該畫面「討論中」；待 `s-board` 或後續收尾任務補上該操作後回頭確認 | Feature: Board 建立與成員邀請 說明句；`ui-authoring-tasks.md` T1.06（`s-board` 已定案內容） | 自動決議 |
| OQ-23 | 2026-09-17 | user-membership | [Level: user-membership/s-member-management] 【推論】`spec-user-membership.md`「Feature: Board 權限管理」Scenario「Member 無法邀請其他成員」寫『When "雅婷" 嘗試邀請 "建宏" 加入這個 Board』『Then 系統應該顯示錯誤訊息 "只有 Owner 可以邀請成員"』，顯示 Member 這個角色會實際觸發一次嘗試並收到錯誤訊息，但未描述 Member 是否原本就看得到 `s-member-management` 這個畫面與其中的成員清單，或這個 Scenario 只在描述後端權限檢查、與畫面呈現無關。 | 1. `r-board-member` 看得到完整成員清單，僅操作被拒絕（本列採用，與 Scenario 描述的「嘗試」動作一致） 2. `r-board-member` 完全看不到本畫面，Scenario 只驗證後端權限檢查 3. 沿用 spec 原文，維持討論中不預設 | 暫定 `r-board-member` 看得到完整成員清單，僅邀請／變更角色操作被拒絕；`s-member-management`「角色與權限」標 ⚠️；維持該畫面「討論中」 | Feature: Board 權限管理 Scenario：Member 無法邀請其他成員 | 自動決議 |
| OQ-24 | 2026-09-17 | user-membership | [Level: user-membership/uc-change-member-role] 【引用原文】`uc-change-member-role` post 僅寫『目標使用者對該 `board` 的角色變更為 Owner』『該 `board` 可以同時有多位 Owner』，未定義將 Owner 降級為 Member 的情境，導致「變更成員角色」操作是否可逆（能否找到另一個操作把狀態改回操作前的樣子）判斷不出。 | 1. 視為不可逆（找不到能降級回 Member 的操作，需確認欄標「是」） 2. 視為可逆（假設之後再變更一次即可改回，需確認欄標「否」，但目前沒有對應 Use Case 支持這個假設） 3. 沿用 spec 原文，維持討論中不預設 | 需確認欄標 ⚠️ 待確認，不猜測是否可逆；`s-member-management`「操作」「資料」段標 ⚠️；維持該畫面「討論中」 | uc-change-member-role post | 自動決議 |
| OQ-25 | 2026-09-17 | user-membership | [Level: user-membership/uc-reject-structure-change-by-member] 【引用原文】`uc-reject-structure-change-by-member` post 寫『系統顯示錯誤訊息「只有 Owner 可以調整看板結構」，`board` 的 Swimlane 數量不變』，其 `crud` 為 `{board: R, board-membership: R}`，對應的是看板結構（Swimlane／Stage）調整被拒絕，操作對象不是成員，不屬於 `s-member-management`（成員管理）畫面的操作範圍。 | 1. 不列入 `s-member-management` 操作表，待確認是否應回頭在 F01 `s-swimlane-list`／`s-stage-list` 的操作失敗欄跨模組引用（本列採用） 2. 仍勉強列入 `s-member-management` 操作表（與畫面「成員管理」的目的不符，不採用） 3. 沿用 spec 原文，維持討論中不預設 | `s-member-management` 不列入本操作，待確認事項標 ⚠️ 回頭確認是否需在 F01 `s-swimlane-list`／`s-stage-list` 跨模組引用；維持該畫面「討論中」 | uc-reject-structure-change-by-member post／crud | 自動決議 |
| OQ-26 | 2026-09-17 | user-membership | [Level: user-membership/s-board-delete-dialog] 【推論】`spec-user-membership.md`「Feature: Board 權限管理」`uc-delete-board` 的 Scenario 只寫『When "雅婷" 嘗試刪除這個 Board』『When 我以 Owner 身分刪除這個 Board』，未描述使用者從哪個既有畫面前往執行刪除；推論入口應與該 `board` 本身相關，可能是 F01 `s-board` 的 Owner 專屬操作，但 `s-board` 操作表（T1.06 已定案內容）目前沒有這一項，與 OQ-22（`s-member-management` 同類缺口）情況相同、但對象是不同 Screen，故另開一列。 | 1. 模組入口（獨立入口，不經看板） 2. 從 F01 `s-board` 的 Owner 專屬操作進入（該操作尚未列在 `s-board` 操作表中，需回頭補上，本列採用） 3. 沿用 spec 原文，維持討論中不預設 | 暫定從 F01 `s-board` 的 Owner 專屬操作進入，`s-board-delete-dialog`「進入與離開」標 ⚠️；維持該畫面「討論中」；待 `s-board` 或後續收尾任務補上該操作後回頭確認 | Feature: Board 權限管理 uc-delete-board Scenario；`ui-authoring-tasks.md` T1.06（`s-board` 已定案內容） | 自動決議 |
| OQ-27 | 2026-09-17 | user-membership | [Level: user-membership/uc-invite-member] 【引用原文】`uc-invite-member` 的 `pre` 僅寫『p1: "邀請者是該 `board` 的 Owner"』『p2: "受邀使用者不是該 `board` 的現有成員"』，未定義受邀對象的帳號是否須為系統中已存在的帳號；`s-member-management`「資料」段原本推論「須為系統中已存在帳號」，但這個限制在 spec 找不到來源。 | 1. 須為系統中已存在帳號（邀請前需先確認帳號存在，本列不採用，找不到 spec 依據） 2. 不要求已存在，允許邀請尚未註冊的帳號（例如以 Email 邀請後續完成註冊，spec 未定義此流程） 3. 沿用 spec 原文，維持討論中不預設 | 依 `uc-invite-member` pre 只引用 p2（不是現有成員），「是否須為系統中已存在帳號」標 ⚠️ 待確認；維持該畫面「討論中」 | uc-invite-member pre | 自動決議 |
| OQ-28 | 2026-09-17 | user-membership | [Level: user-membership/s-cards-by-assignee] 【推論】`spec-user-membership.md`「Feature: 卡片負責人指派」Scenario「一位成員同時是多張卡片的負責人」的 When 步驟僅寫『我查看 "雅婷" 負責的卡片清單』，未描述使用者從哪個既有畫面前往查看指定成員的負責卡片清單；推論入口可能與成員管理相關，但 `s-member-management` 操作表（T2.06 已定案內容）未列出對應操作，F05 `ui-workload.md` 尚未撰寫，無法確認是否從該處進入。 | 1. 從 `s-member-management` 成員清單列點擊成員查看（該操作尚未列在 `s-member-management` 操作表中，需回頭補上） 2. 從 F05 工作量檢視畫面進入（該模組 ui 檔尚未撰寫，本輪無法確認） 3. 模組獨立入口（不經其他畫面） 4. 沿用 spec 原文，維持討論中不預設 | 維持討論中，不預設入口，`s-cards-by-assignee`「從哪裡進來」標 ⚠️；待 `s-member-management` 收尾或 F05 `ui-workload.md` 撰寫後回頭確認 | Feature: 卡片負責人指派 Scenario：一位成員同時是多張卡片的負責人 | 自動決議 |
| OQ-29 | 2026-09-17 | user-membership | [Level: user-membership/s-activity-log] 【引用原文】`spec-user-membership.md`「Feature: 檢視看板活動紀錄」Feature 說明明文寫『目前「kanban-core」每個 aggregate 只管自己的活動紀錄，還沒有一個地方把 Board、每張 Card、BoardMembership 的事件合併成一份、依時間排序的統一列表』，且『這件事本質上是跨 aggregate 的查詢投影，留給「kanban-spring」有 application／persistence 層時再做』『本情境目前尚未實作』，對應 `uc-view-board-activity-log` 尚無可推導的後端行為。 | 1. 依 T2.10 分支規則，畫面狀態維持「未討論」，僅保留八段標題與簡短說明理由，不產出完整內容（本列採用） 2. 比照其他畫面正常定案（找不到可推導的 pre／post 內容，不採用） 3. 直接刪除本畫面骨架（spec 已定義對應 uc，不應刪除，不採用） | `s-activity-log` 狀態維持「未討論」，八段標題各填「不適用，理由同上」並於「待確認事項」標 ⚠️；待「kanban-spring」補上跨 aggregate 查詢投影、spec 更新後回頭定案 | Feature: 檢視看板活動紀錄 說明段落 | 自動決議 |
| OQ-30 | 2026-09-17 | user-membership | [Level: kanban-basic/s-card-add-dialog（跨模組，見 user-membership `uc-member-add-card`）] 【引用原文】F01 `uc-add-card` 的 roles 為 r-user、crud 為 board:R、card:C（不逐字引用，僅描述既有定義），`spec-user-membership.md` `uc-member-add-card` 寫『roles: [r-board-member]』『crud: {board-membership: R, card: C}』，post 為『新的 `card` 建立成功』；「角色定義」表另寫 `r-board-member` 『被加入 Board 的人，只能新增／編輯／移動／刪除卡片與留言，不能碰成員管理、看板結構（Swimlane/Stage）或刪除 Board』。兩個 uc 是否為同一個新增卡片動作在不同角色下的表述、還是各自獨立的寫入動作，spec 沒有寫清楚；`ui-check` 的 DS-05 要求同一操作列的可用角色須與所觸發 uc 的 roles 完全一致，而兩者 roles 互斥（皆為單一角色），無法在 `s-card-add-dialog` 同一列「確認新增」操作同時掛兩個 uc。 | 1. 視為同一動作：回頭修正 F01 `uc-add-card` 的 roles（例如改為包含 r-board-member 或改沿用 r-board-member），使其與 `uc-member-add-card` 一致，但這是動到定稿文字，需先開 CR 2. 視為獨立的兩個寫入動作：在 `s-card-add-dialog` 新增 `r-board-member` 角色列，並拆出第二列操作（例如另一個觸發 `uc-member-add-card` 的列），兩列各自角色與 uc 對齊 3. 維持現狀不處理，僅標 ⚠️ 待確認，`s-card-add-dialog` 維持「討論中」（本列採用） | 維持現狀，`s-card-add-dialog`「狀態」段「無權限」與「待確認事項」標 ⚠️ 說明此矛盾，畫面維持「討論中」；是否修正 uc-add-card roles（需 CR）或拆分操作列，待人工決定 | F01 uc-add-card；`spec-user-membership.md` uc-member-add-card；角色定義表 r-board-member | 自動決議 |
| OQ-31 | 2026-09-17 | kanban-widgets | [Level: kanban-widgets/s-cycle-lead-time-dashboard] 【引用原文】`spec-kanban-widgets.md`「Feature: Cycle Time 與 Lead Time 分析」Background 只寫『Given 我已登入系統，並開啟 Board "產品開發看板"』，未描述使用者從哪個既有畫面前往開啟本圖表；跨模組 F01 `s-board` 操作表（T1.06 已定案內容）目前只有拖曳卡片、新增／開啟／刪除卡片、管理 Swimlane／Stage 幾項操作，沒有「檢視圖表」這一項。 | 1. 從 F01 `s-board` 新增一項「檢視圖表」操作進入（該操作尚未列在 `s-board` 操作表中，需回頭補上，本列採用） 2. 模組獨立入口（不經 `s-board`） 3. 沿用 spec 原文，維持討論中不預設 | 暫定從 F01 `s-board` 的操作進入，`s-cycle-lead-time-dashboard`「進入與離開」標 ⚠️；維持該畫面「討論中」；待 `s-board` 收尾任務補上該操作後回頭確認，同一個入口缺口預期會在 `s-wip-dashboard`／`s-throughput-cfd-dashboard`／`s-duedate-reminder` 重複出現 | Feature: Cycle Time 與 Lead Time 分析 Background；`ui-authoring-tasks.md` T1.06（`s-board` 已定案內容） | 自動決議 |
| OQ-32 | 2026-09-17 | kanban-widgets | [Level: kanban-widgets/s-duedate-reminder] 【引用原文】`uc-view-duedate-reminder` post p2 寫『截止日期與看板目前時間相差在門檻天數內、尚未完成的 `card` 顯示於「即將到期」清單，門檻天數由使用者於查詢時設定』，Scenario「檢視即將到期的卡片」也只寫『And 即將到期的門檻設定為 3 天』，皆未定義門檻天數的型別／範圍（例如是否限正整數、有無上限）。 | 1. 限正整數，不設上限（本列不採用，找不到 spec 依據） 2. 資料表「驗證／格式」欄標 ⚠️ 待確認，不自行假設型別／範圍（本列採用） 3. 沿用 spec 原文，維持討論中不預設 | 資料表「驗證／格式」欄標 ⚠️ 待確認：型別／範圍 spec 未定義，僅有範例值 3 天；`s-duedate-reminder`「待確認事項」加一行指向本列；維持該畫面「討論中」 | uc-view-duedate-reminder post p2；Scenario：檢視即將到期的卡片 | 自動決議 |
| OQ-33 | 2026-09-17 | kanban-widgets | [Level: kanban-widgets/s-cycle-lead-time-dashboard] 【引用原文】`uc-view-cycle-lead-time` post p2 寫『統計摘要的平均值與百分位計算排除該 `card`，並顯示排除計算的卡片數』，未指定「百分位」具體是哪些百分位數（例如 P50／P85／P95）。另外，post p2 這句「統計摘要」未區分 Lead Time 與 Cycle Time，但 Scenario「沒有經過 Start 就完成的卡片，Cycle Time 不計入統計」僅寫『統計摘要的 Cycle Time 平均值與百分位計算應該排除卡片 "B"』，只驗證 Cycle Time 統計排除，未提及 Lead Time 統計是否也比照排除；【推論】採用 Scenario 較具體的敘述，本列一併記錄此落差，不自行假設 Lead Time 統計也套用排除規則。 | 1. 具體百分位數與 Lead Time 排除範圍由後端另行定義，ui 檔直接假設（不採用，找不到 spec 依據） 2. 資料表「驗證／格式」欄標 ⚠️ 待確認百分位數，「來源」欄僅描述 Cycle Time 統計排除規則，不自行假設 Lead Time 統計也排除（本列採用） 3. 沿用 spec 原文，維持討論中不預設 | 資料表「統計摘要」列「驗證／格式」欄標 ⚠️ 待確認：具體百分位數 spec 未定義；「來源」欄改為僅描述 Cycle Time 統計排除規則，Lead Time 統計是否比照排除另標 ⚠️；`s-cycle-lead-time-dashboard`「待確認事項」與「驗收條件」對應處指向本列；維持該畫面「討論中」 | uc-view-cycle-lead-time post p2；Scenario：沒有經過 Start 就完成的卡片，Cycle Time 不計入統計 | 自動決議 |
| OQ-34 | 2026-09-17 | canvas-layout | [Level: canvas-layout/s-cycle-lead-time-dashboard、s-wip-dashboard、s-throughput-cfd-dashboard、s-duedate-reminder（跨模組，推翻 OQ-31，影響 F03 圖表模組四個畫面）] 【矛盾】A：OQ-31 依 F03 圖表 Feature 的 Background（僅寫已開啟 Board）暫定四個圖表畫面「從 F01 `s-board` 新增一項「檢視圖表」操作進入」。B：`spec-canvas-layout.md` 簡介寫『使用者開啟某個 Board 時，看到的就是該 Board 的 Canvas』，且『元件本身的內容與行為（例如圖表的資料、設定、互動）不屬於本模組，由元件所屬模組定義』；人工已於 OQ-17／OQ-18／OQ-19 確認看板本體與其他元件改以 F07 `item` 形式呈現於 `s-canvas`，不透過 `s-board` 操作表進入。A、B 對「圖表畫面如何被進入」互相衝突：A 暗示新增 `s-board` 操作列，B 暗示圖表未來可能是 Canvas 上的元件、入口由 F07 決定，而非 `s-board` 操作表。本列推翻 OQ-31 的「暫定從 `s-board` 進入」。 | 保留A：沿用 OQ-31，從 F01 `s-board` 新增操作列進入 保留B：進入路徑由 F07 Canvas／`item` 機制決定，不透過 `s-board` 操作表 缺區分條件：兩處都不採用，維持討論中，待補充能區分適用情境的規則（本列採用） | 進入路徑不預設，待 D-09／F07 整合 CR 由人工決定 | `spec-canvas-layout.md` 簡介；OQ-31；OQ-17；OQ-18；OQ-19；D-09 | 自動決議 |
| OQ-35 | 2026-09-17 | board-clock | [Level: board-clock/s-board-clock-control] 【推論】`spec-board-clock.md`「Feature: 看板時間管理」Background 只寫『Given 我已登入系統，並開啟 Board "產品開發看板"』『And 看板時間目前為 2026-09-12 11:00:00』，未描述使用者從哪個既有畫面前往開啟看板時鐘控制畫面；與 OQ-31（F03 圖表畫面同類缺口，且已被 OQ-34 推翻為「進入路徑由 F07 Canvas 機制決定」）情況相同，`s-board-clock-control` 很可能面臨相同的 F01 `s-board` 操作表 vs. F07 Canvas `item` 機制的入口歸屬問題。 | 1. 從 F01 `s-board` 新增一項操作進入（與 OQ-31 原結論同類，但已被 OQ-34 推翻，風險是重蹈覆轍） 2. 進入路徑由 F07 Canvas／`item` 機制決定，不透過 `s-board` 操作表（與 OQ-34 一致） 3. 模組獨立入口（不經 `s-board`） 4. 沿用 spec 原文，維持討論中不預設（本列採用，比照 OQ-34 缺區分條件的結論，避免與 F07 整合方向衝突） | 進入路徑不預設，`s-board-clock-control`「進入與離開」標 ⚠️；維持該畫面「討論中」；待 D-09／F07 整合 CR 或 F04 專屬入口決議後回頭確認 | Feature: 看板時間管理 Background；OQ-31；OQ-34；D-09 | 自動決議 |
| OQ-36 | 2026-09-17 | board-clock | [Level: board-clock/s-board-clock-control] 【引用原文】`uc-adjust-board-clock` post 寫『`board` 的看板時間更新為指定時間』，`uc-pause-resume-board-clock` post 寫『暫停時，`board` 的看板時間狀態切換為 PAUSED，看板時間停在暫停當下的時間』『恢復時，`board` 的看板時間狀態切換為 REALTIME，看板時間從暫停時的時間繼續隨系統時間前進』，皆只用「看板時間」文字描述，`spec-board-clock.md`「名詞定義」的實體表、欄位表皆為空表，僅「其他名詞」表用文字描述 Board Clock（看板時鐘）：『某個 Board 專屬的「現在」時間來源，取代系統時間（wall clock）作為該 Board 底下所有事件的「occurredAt」』，找不到可供反引號引用的 Attribute ID。 | 1. 資料表以文字描述看板時間相關欄位，不掛反引號 ID，並標 ⚠️ 回饋後端 Spec 補上對應欄位定義（本列採用，同 OQ-09／OQ-14 處理方式） 2. 自行在 ui 檔定義一個新的欄位 ID 供反引號引用（違反鐵則 1「不定義新概念」，不採用） 3. 資料表不列看板時間相關欄位（不符合 `ui-convention.md` 要求資料表列出畫面顯示欄位） | 資料表以文字描述看板時間目前值／狀態／調整目標時間欄位，不掛反引號 ID，「待確認事項」標 ⚠️ 回饋後端 Spec 補上欄位定義；維持該畫面「討論中」 | uc-adjust-board-clock post；uc-pause-resume-board-clock post；名詞定義 實體表／欄位表（空表）、其他名詞表 Board Clock 列 | 自動決議 |
| OQ-37 | 2026-09-17 | board-clock | [Level: board-clock/s-board-clock-control] 【引用原文】`uc-adjust-board-clock`／`uc-pause-resume-board-clock` 皆寫『roles: [r-board-owner]』，`uc-adjust-board-clock` fail p1 寫『拒絕，顯示錯誤訊息「只有 Owner 可以調整看板時間」，`board` 的看板時間維持不變』，僅定義 Owner 可操作與非 Owner 操作被拒絕，未描述非 Owner（例如僅有 `r-user` 身分者）是否仍看得到本畫面或看板時間目前值。 | 1. `r-user` 看得到看板時間目前值與狀態，僅操作被拒絕（比照 OQ-23 `s-member-management` 的判斷方式） 2. `r-user` 完全看不到本畫面 3. 沿用 spec 原文，維持討論中不預設（本列採用，因為與 OQ-23 情境不同——OQ-23 有 Scenario 描述「嘗試」動作，本畫面沒有對應 Scenario 可佐證） | `s-board-clock-control`「角色與權限」「狀態」段的無權限欄標 ⚠️；維持該畫面「討論中」 | uc-adjust-board-clock roles、fail p1 | 自動決議 |
| OQ-38 | 2026-09-17 | board-clock | [Level: board-clock/uc-adjust-board-clock] 【引用原文】Scenario「看板時間可以往回調整」的 When 步驟寫『When 我將看板時間調整為 2026-09-12 12:00:00』，`uc-adjust-board-clock` post 僅寫『`board` 的看板時間更新為指定時間』，未定義調整目標時間輸入的型別／範圍（例如是否限制不可早於某個時間點、格式為何），Scenario 範例值皆為完整日期時間，但沒有明確的欄位限制描述。 | 1. 限制不可早於某個時間點（本列不採用，找不到 spec 依據；`uc-adjust-board-clock` 允許往回調，僅下游建立新事件時受 `uc-guard-clock-monotonicity` 限制） 2. 型別／範圍不限，資料表「驗證／格式」欄標 ⚠️ 待確認，不自行假設（本列採用，同 OQ-32 處理方式） 3. 沿用 spec 原文，維持討論中不預設 | 資料表「調整目標時間」列「驗證／格式」欄標 ⚠️ 待確認：型別／範圍 spec 未定義，僅有 Scenario 範例值；維持該畫面「討論中」 | uc-adjust-board-clock post；Scenario：看板時間可以往回調整 | 自動決議 |
| OQ-39 | 2026-09-17 | canvas-layout | [Level: canvas-layout/s-cycle-lead-time-dashboard、s-wip-dashboard、s-throughput-cfd-dashboard、s-duedate-reminder（跨模組，修正 OQ-34「情況」欄敘述範圍，影響 F03 圖表模組四個畫面，對應 D-19）] 【推論】OQ-34「情況」欄寫「人工已於 OQ-17／OQ-18／OQ-19 確認看板本體與其他元件改以 F07 item 形式呈現於 s-canvas，不透過 s-board 操作表進入」，但覆核 OQ-17／OQ-18／OQ-19 逐字內容：OQ-17 只針對新增 Swimlane／新增 Stage 的觸發位置做決定；OQ-18 只針對看板本體（`s-board`）本身，依 `spec-canvas-layout.md` 簡介『使用者開啟某個 Board 時，看到的就是該 Board 的 Canvas』做決定；OQ-19 只針對看板成員頭像清單做決定；三列皆未對 F03 圖表畫面或看板之外的其他元件做過決定，`spec-canvas-layout.md` 簡介另寫『元件本身的內容與行為（例如圖表的資料、設定、互動）不屬於本模組，由元件所屬模組定義』，本身也未指明圖表是否會成為 `item`。OQ-34「情況」欄把已確認範圍說大了，本列予以修正：F03 圖表是否成為 F07 `item`，人工尚未決定。 | 1. 維持 OQ-34 原措辭不修正（不採用，範圍描述有誤，可能誤導後續判斷 F03 圖表入口已定案） 2. 追加新列修正措辭，明示僅 OQ-17／OQ-18／OQ-19 實際確認範圍，並寫明「F03 圖表是否成為 item，人工尚未決定」（本列採用；OQ 檔只能追加，不可編輯 OQ-34 本身，做法比照 OQ-34 推翻 OQ-31 的既有模式） 3. 直接編輯 OQ-34 原文字（不採用，違反 OQ 檔只能追加規則，本輪已示範修改既有列會導致 check-oq「舊內容被修改或刪除」失敗） | 選項 2：本列作為 OQ-34「情況」欄的範圍修正記錄；F03 圖表是否成為 F07 `item` 維持討論中不預設，待 D-09／F07 整合 CR 由人工決定（與 OQ-34 原結論一致，僅收斂敘述範圍，不改變原決議方向） | OQ-34；OQ-17；OQ-18；OQ-19；`spec-canvas-layout.md` 簡介；D-19 | 自動決議 |
| OQ-40 | 2026-09-17 | board-clock | [Level: board-clock/s-board-clock-control（推翻 OQ-35「情況」欄敘述，對應 D-21）] 【推論】OQ-35「情況」欄以「Feature: 看板時間管理」Background『Given 我已登入系統，並開啟 Board "產品開發看板"』未描述進入路徑為由，類比 OQ-31／OQ-34 的 F03 情況，寫「與 OQ-31（F03 圖表畫面同類缺口，且已被 OQ-34 推翻為「進入路徑由 F07 Canvas 機制決定」）情況相同」；但覆核 OQ-34「採用」欄實際文字為「進入路徑不預設，待 D-09／F07 整合 CR 由人工決定」（對應選項 3「缺區分條件」），OQ-34 並未採用「進入路徑由 F07 Canvas 機制決定」（該文字是 OQ-34 選項 2「保留B」的措辭，未被採用）。OQ-35「情況」欄把 OQ-34 的結論誤述為已選定某個具體方向，本列予以修正：OQ-34 實際結論是「進入路徑不預設」，不是「由 F07 Canvas 機制決定」。 | 1. 維持 OQ-35 原措辭不修正（不採用，誤述 OQ-34 結論，可能誤導後續判斷已選定 F07 Canvas 機制） 2. 追加新列修正措辭，明示 OQ-34 實際採用「進入路徑不預設」而非「由 F07 Canvas 機制決定」（本列採用；OQ 檔只能追加，不可編輯 OQ-35 本身，比照 OQ-39 修正 OQ-34 的既有模式） 3. 直接編輯 OQ-35 原文字（不採用，違反 OQ 檔只能追加規則） | 選項 2：本列作為 OQ-35「情況」欄的敘述修正記錄；OQ-35「採用」欄（選項 4，進入路徑不預設）不變，與本列修正後的理解一致 | OQ-34；OQ-35；D-21 | 自動決議 |
| OQ-41 | 2026-09-17 | workload | [Level: workload/s-workload-dashboard] 【引用原文】`spec-workload.md`「Feature: 人員工作量檢視」Background 只寫『Given 我已登入系統，並開啟 Board "產品開發看板"』，未描述使用者從哪個畫面或操作前往 Workload 表，與 OQ-31／OQ-35 等其他模組儀表板／控制項畫面同類缺口。 | 1. 模組入口（獨立入口，不經看板） 2. 從 F01 `s-board` 或其他既有畫面新增一項操作進入 3. 進入路徑不預設，維持討論中不預設（本列採用，比照 OQ-31／OQ-34／OQ-35／OQ-40 同類問題的處理方式） | 進入路徑不預設，`s-workload-dashboard`「從哪裡進來」標 ⚠️；維持該畫面「討論中」 | Feature: 人員工作量檢視 Background；OQ-31；OQ-34；OQ-35；OQ-40 | 自動決議 |
| OQ-42 | 2026-09-17 | workload | [Level: workload/uc-drag-assign-card-owner] 【引用原文】`spec-workload.md`「Feature: 人員工作量檢視」Scenario「拖曳成員頭像到卡片上，追加該成員為負責人」的 When 步驟寫『When 我將 "雅婷" 的頭像拖曳到卡片 "B" 上』，但 `uc-view-workload` post 只描述依成員分組的工作量「數量」與未指派卡片「數量」，Background 與 post 均未描述 Workload 表本身是否顯示個別卡片清單，供拖曳頭像時作為拖曳目標。 | 1. 本畫面另外顯示個別卡片清單（例如依成員展開）作為拖曳目標（風險：spec 未定義清單欄位來源與呈現方式） 2. 拖曳目標卡片顯示在其他畫面（例如 F01 `s-board`），本畫面只作為拖曳來源，卡片清單非本畫面資料 3. 沿用 spec 原文，維持討論中不預設（本列採用） | `s-workload-dashboard`「資料」段「卡片標題」列與「待確認事項」標 ⚠️ 待確認；維持該畫面「討論中」 | uc-view-workload post；Scenario：拖曳成員頭像到卡片上，追加該成員為負責人 | 自動決議 |
| OQ-43 | 2026-09-17 | feature-cr-board | [Level: feature-cr-board/s-feature-cr-board] 【引用原文】`spec-feature-cr-board.md`「Feature: Feature／CR 追蹤表」Background 只寫『Given 我已登入系統，並開啟 Board "產品開發看板"』，未描述使用者從哪個畫面或操作前往 Feature／CR 追蹤表，與 OQ-31／OQ-41 等其他模組儀表板畫面同類缺口。 | 1. 模組入口（獨立入口，不經看板） 2. 從 F01 `s-board` 或其他既有畫面新增一項操作進入 3. 進入路徑不預設，維持討論中不預設（本列採用，比照 OQ-31／OQ-34／OQ-35／OQ-40／OQ-41 同類問題的處理方式） | 進入路徑不預設，`s-feature-cr-board`「從哪裡進來」標 ⚠️；維持該畫面「討論中」 | Feature: Feature／CR 追蹤表 Background；OQ-31；OQ-41 | 自動決議 |
| OQ-44 | 2026-09-17 | canvas-layout | [Level: canvas-layout/s-canvas] 【引用原文】`spec-canvas-layout.md`「角色定義」表定義 `r-canvas-editor`（說明『可放置與排列畫布元素，並平移與縮放自己的檢視區』）與 `r-canvas-viewer`（說明『只能平移與縮放自己的檢視區，不可改動元素』），但本文件（含「待釐清」）未描述這兩個角色如何對應到另一模組「使用者與看板成員」規格「角色定義」表定義的 `r-system-user`／`r-board-owner`／`r-board-member`；同時 `s-canvas` 需引用 F01 `uc-add-swimlane`／`uc-add-stage`，其 `roles: [r-user]`（F01 角色）與 `r-canvas-editor`／`r-canvas-viewer` 也無對應關係定義，與 OQ-06（F02 `r-user` 與 `r-system-user`／`r-board-owner`／`r-board-member` 同類缺口）同一類問題。 | 完整角色清單：F07 `r-canvas-editor`／`r-canvas-viewer`；F01 `r-user`；F02 `r-system-user`／`r-board-owner`／`r-board-member`。1. `r-canvas-editor`＝`r-board-member`（含 Owner），`r-canvas-viewer` 無對應角色（皆不採用，因 F02 不存在唯讀看板角色概念） 2. 不預設對應關係，`r-canvas-editor`／`r-canvas-viewer`／`r-user` 視為各模組獨立角色，暫不建立跨模組對應（本列採用） 3. 沿用 spec 原文，維持討論中不預設 | 不預設對應關係，`s-canvas`「角色與權限」表僅使用 spec 已定義的 `r-canvas-editor`／`r-canvas-viewer`／`r-user`（F01，跨模組），「待確認事項」標 ⚠️ 回饋此缺口；維持該畫面「討論中」 | 「角色定義」表 `r-canvas-editor`／`r-canvas-viewer` 說明；F01 `uc-add-swimlane`／`uc-add-stage` roles；F02 使用者與看板成員規格「角色定義」表；OQ-06 | 自動決議 |
| OQ-45 | 2026-09-18 | canvas-layout | [Level: canvas-layout/s-canvas（跨模組，推翻 OQ-22，影響 F02 的 `s-member-management`）] 【推論】人工於本輪確認：延續 OQ-19 已建立的決策（「看板成員頭像清單」是 F07 `s-canvas` 上一個獨立的 `item`，不是 F01 `s-board` 的一部分），`spec-canvas-layout.md` 簡介『元件本身的內容與行為（例如圖表的資料、設定、互動）不屬於本模組，由元件所屬模組定義』，人工進一步確認使用者從這個「看板成員」`item` 上選擇加入成員的操作，以此進入 `s-member-management`，不是從 F01 `s-board` 的操作進入。推翻 OQ-22（原「暫定從 F01 `s-board` 的 Owner 專屬操作進入」）。 | 1. 維持 OQ-22：從 F01 `s-board` 的 Owner 專屬操作進入 2. 從 F07 `s-canvas` 上的「看板成員」`item`（OQ-19 建立）選擇加入成員的操作進入（本列採用） 3. 沿用 spec 原文，維持討論中不預設 | 採用選項 2：`s-member-management`「從哪裡進來」改為「F07 `s-canvas` 上「看板成員」item 選擇加入成員的操作」，「中途放棄會怎樣」的目的地同步改為回到該 item／`s-canvas`；機制細節（`item` 上如何觸發加入成員）待 T7.02 撰寫 `s-canvas` 時一併判斷，暫不變更 `s-canvas` 尚未定案前的具體措辭，先移除 OQ-22 的 ⚠️ | `spec-canvas-layout.md` 簡介；`ui-authoring-open-questions.md` OQ-19；F02 UI 短規格的 `s-member-management` | 自動決議 |
| OQ-46 | 2026-09-18 | user-membership | [Level: user-membership/uc-reject-structure-change-by-member（推翻 OQ-25，影響 F01 的 9 個結構調整 usecase）] 【引用原文】人工於本輪直接修正 spec 解決角色模型不一致（不是 loop 自動決議）：`spec-user-membership.md` 的 `uc-reject-structure-change-by-member` `pre` 寫『操作者不是該 `board` 的 Owner』，F01 spec 的 `uc-add-swimlane`／`uc-rename-swimlane`／`uc-reorder-swimlane`／`uc-delete-swimlane`／`uc-add-stage`／`uc-rename-stage`／`uc-reorder-stage`／`uc-delete-stage`／`uc-set-stage-role` 這 9 個結構調整 usecase 的 `roles` 已從 r-user 改為 r-board-owner，對齊這個權限模型。推翻 OQ-25（原「待確認是否回頭在 F01 `s-swimlane-list`／`s-stage-list` 的操作失敗欄跨模組引用」）。 | （角色模型已由人工修正對齊，不再需要三選項） | 已修正 F01 spec 9 個 usecase 的 `roles`；F01 `ui-kanban-basic.md` 的 `s-swimlane-list`／`s-swimlane-delete-dialog`／`s-stage-list`／`s-stage-delete-dialog` 角色與權限表改為 `r-board-owner`，`s-swimlane-list`／`s-stage-list`「無權限」狀態新增一行引用 `uc-reject-structure-change-by-member`（F02，跨模組）；F07 `ui-canvas-layout.md` 的 `s-canvas` 角色與權限表同步改為 `r-board-owner`（F02，跨模組）；`s-member-management` 移除 OQ-25 的 ⚠️ | `uc-reject-structure-change-by-member` pre；F01 spec 變更紀錄 | 自動決議 |
| OQ-47 | 2026-09-18 | user-membership | [Level: user-membership/s-board-list（推翻 OQ-26）] 【推論】人工於本輪確認：刪除 Board 屬於 Board 列表層級的管理動作（跟已經定為 F07 canvas 層級動作的成員管理不同），使用者從 `s-board-list` 每一列 Board 直接觸發「刪除」，不需要先進入 Board 的 Canvas。推翻 OQ-26（原「暫定從 F01 `s-board` 的 Owner 專屬操作進入」）。 | 1. 從 `s-board-list` 每一列加一個「刪除」操作進入（本列採用） 2. 先進入 Canvas，再從裡面觸發刪除 3. 沿用 spec 原文，維持討論中不預設 | 採用選項 1：`s-board-list` 操作表新增「刪除 Board」列（`r-board-owner` 可用），`s-board-delete-dialog`「從哪裡進來」改為「`s-board-list` 的『刪除 Board』操作」，移除 OQ-26 的 ⚠️，畫面狀態改為「已定案」 | `ui-user-membership.md` `s-board-list`／`s-board-delete-dialog` | 自動決議 |
| OQ-48 | 2026-09-18 | user-membership | [Level: user-membership/uc-change-member-role（確認 OQ-24）] 【引用原文】人工於本輪確認：`uc-change-member-role` `pre` 寫『p2: "目標使用者是該 `board` 的 Member"』，`post` 只定義『目標使用者對該 `board` 的角色變更為 Owner』，人工決定維持單向（不新增降級用例），這代表「變更成員角色」找不到任何操作能復原，即為不可逆。確認 OQ-24：需確認欄標「是」。 | （可逆性已由人工確認，不再需要三選項） | `s-member-management` 操作表「變更成員角色」需確認欄改為「是」；資料表「變更後角色」移除 ⚠️；待確認事項移除 OQ-24；畫面狀態改為「已定案」 | uc-change-member-role pre p2／post | 自動決議 |
| OQ-49 | 2026-09-18 | canvas-layout | [Level: canvas-layout/s-canvas（跨模組，推翻 OQ-31／OQ-34／OQ-39／OQ-35／OQ-40／OQ-41／OQ-43，影響 F03 四個儀表板畫面、F04 時鐘控制畫面、F05 工作量儀表板、F06 追蹤表畫面）] 【推論】人工於本輪確認：延續 OQ-17／OQ-18／OQ-19 的 F07 類 Miro 畫布方向，`spec-canvas-layout.md` 簡介『使用者開啟某個 Board 時，看到的就是該 Board 的 Canvas』，且『元件本身的內容與行為（例如圖表的資料、設定、互動）不屬於本模組，由元件所屬模組定義』，人工進一步確認 F03 的 Cycle/Lead Time、WIP、Throughput/CFD、截止日期提醒、F04 的時鐘控制、F05 的人員工作量儀表板、F06 的 Feature/CR 追蹤表，這些原本各自獨立的儀表板／控制項畫面，全部都跟看板本體一樣是 F07 `s-canvas` 上可放置的獨立 `item`，使用者透過畫布的「放置元件」操作（`uc-place-item`）把它們加入畫布，不是從某個既有畫面的操作列被「導向進入」。推翻 OQ-31／OQ-34／OQ-39（F03 四個畫面「進入路徑不預設」）、OQ-35／OQ-40（F04 時鐘控制畫面同上）、OQ-41（F05 工作量儀表板同上）、OQ-43（F06 追蹤表畫面同上）。 | 1. 維持原結論：各自從 F01 `s-board` 或其他既有畫面新增操作列進入 2. 全部改為 F07 `s-canvas` 上可放置的獨立 `item`，比照看板本體（本列採用） 3. 沿用 spec 原文，維持討論中不預設 | 採用選項 2：F03 四個畫面、F04 時鐘控制畫面、F05 工作量儀表板、F06 追蹤表畫面的「從哪裡進來」皆改為「不適用——內容以 F07 item 形式顯示於 s-canvas，見 spec-canvas-layout.md；如何新增此類元件的具體機制（item.component 對應值等）仍待該 spec 「待釐清」與整合 CR 定案」，移除各自對應的進入路徑 ⚠️；沒有其他 ⚠️ 的畫面同步改為「已定案」 | `spec-canvas-layout.md` 簡介；OQ-17／OQ-18／OQ-19 | 自動決議 |
| OQ-50 | 2026-09-18 | workload | [Level: workload/uc-drag-assign-card-owner（確認 OQ-42）] 【引用原文】人工於本輪確認：`spec-workload.md`「Feature: 人員工作量檢視」usecase 區塊後方說明，拖曳追加負責人的兩個 Scenario 與 F02「卡片負責人指派」『Feature 的 `uc-assign-card-owner-by-drag` 是同一個操作』，只是因工具限制（GH-01 要求 tag 只能指向同一 Feature 內的 usecase）另立 usecase id；依 OQ-49，`s-workload-dashboard` 與 F01 `s-board` 現在都是可同時顯示於 F07 `s-canvas` 的獨立 item，拖放目標卡片顯示在 `s-board` item 上，`s-workload-dashboard` 本身不需要另外列出個別卡片清單。確認 OQ-42：採選項 2。 | （已由人工確認，不再需要三選項） | `s-workload-dashboard` 資料表移除「卡片標題」列；操作表「拖曳成員頭像到卡片追加負責人」列說明拖放目標為同一畫布上的 F01 `s-board` item；移除 OQ-42 的 ⚠️；畫面狀態改為「已定案」 | spec-workload.md「拖曳追加負責人」段落說明；OQ-49 | 自動決議 |
| OQ-51 | 2026-09-18 | workload | [Level: workload/s-workload-dashboard（推翻 OQ-28）] 【推論】人工於本輪確認：`spec-workload.md` `uc-view-workload` post 寫『依 `board-membership` 成員分組統計各成員擔任負責人的 Active Card 數量，作為該成員的 Workload』，F05 `s-workload-dashboard` 已定案顯示各成員的工作量數字，點擊該數字是查看該成員負責卡片清單最自然的入口，故 F02 `s-cards-by-assignee` 從 `s-workload-dashboard` 的「點擊成員工作量查看卡片清單」操作進入。推翻 OQ-28（原「維持討論中，不預設入口，待 `s-member-management` 收尾或 F05 `ui-workload.md` 撰寫後回頭確認」）。 | 1. 從 `s-member-management` 成員清單列點擊查看 2. 從 F05 `s-workload-dashboard` 點擊成員工作量查看（本列採用） 3. 模組獨立入口 4. 沿用 spec 原文，維持討論中不預設 | 採用選項 2：`s-workload-dashboard` 操作表新增「點擊成員工作量查看卡片清單」列，`s-cards-by-assignee`「從哪裡進來」改為該操作，移除 OQ-28 的 ⚠️，畫面狀態改為「已定案」 | `ui-workload.md` `s-workload-dashboard`；F02 UI 短規格的 `s-cards-by-assignee` | 自動決議 |
| OQ-52 | 2026-09-18 | kanban-widgets | [Level: kanban-widgets/uc-view-duedate-reminder（確認 OQ-32）] 【引用原文】人工於本輪確認：`spec-kanban-widgets.md` `uc-view-duedate-reminder` 的 `pre` 已補上『指定的門檻天數（用於比較 `card.due-date`）為 1 到 365 之間的正整數』，且 F03 六個檢視類 usecase 的角色一併從 r-user 改為 r-board-member（同一類「檢視儀表板」動作，邏輯應一致，比照 OQ-06／OQ-25 的處理方式）。確認 OQ-32。 | （已由人工確認，不再需要三選項） | `uc-view-duedate-reminder` 新增 pre p1（門檻天數為 1 到 365 之間的正整數）與對應 fail、Scenario；F03 六個 usecase 的 roles 皆改為 `r-board-member`；`s-duedate-reminder` 資料表補上驗證規則、操作表補上失敗情境，四個畫面角色表同步更新；`s-duedate-reminder` 移除 OQ-32 的 ⚠️，畫面狀態改為「已定案」 | spec-kanban-widgets.md 變更紀錄 2026-09-18 | 自動決議 |
| OQ-53 | 2026-09-18 | kanban-widgets | [Level: kanban-widgets/uc-view-cycle-lead-time（確認 OQ-33）] 【引用原文】人工於本輪確認：`uc-view-duedate-reminder` 之外，`spec-kanban-widgets.md` `uc-view-cycle-lead-time` post 已補上『Cycle Time、Lead Time 的統計摘要，其百分位計算固定顯示 `card` 清單中 P50、P85、P95 三個百分位數』，並明訂 Lead Time 統計不排除任何卡片（Lead Time 從建立到完成，不會有「無」的情況，不同於 Cycle Time）。確認 OQ-33。 | （已由人工確認，不再需要三選項） | `uc-view-cycle-lead-time` post 新增百分位固定值與 Lead Time 排除規則說明，並補上對應 Scenario 斷言；`s-cycle-lead-time-dashboard` 資料表「統計摘要」列更新，移除 OQ-33 的 ⚠️，畫面狀態改為「已定案」 | spec-kanban-widgets.md 變更紀錄 2026-09-18（`uc-view-cycle-lead-time` 統計摘要細節） | 自動決議 |
| OQ-54 | 2026-09-18 | board-clock | [Level: board-clock/uc-adjust-board-clock（確認 OQ-36）] 【引用原文】人工於本輪確認：`spec-board-clock.md` 已補上 `board.clock-time`、`board.clock-status` 兩個欄位，`uc-adjust-board-clock`／`uc-pause-resume-board-clock` 的 post／fail 已改為引用『`board.clock-time` 更新為指定時間』這類含 Attribute ID 的句子。確認 OQ-36，並推翻 OQ-36、推翻 OQ-37、推翻 OQ-38 各自引用的舊 post／fail 逐字原文（結論不變，僅原文措辭因本次修正而更新，OQ-37／OQ-38 本身待確認的問題仍未解決）。 | （已由人工確認，不再需要三選項） | `spec-board-clock.md` 新增 `board.clock-time`／`board.clock-status` 欄位；`s-board-clock-control` 資料表三列改填這兩個 Attribute ID，移除 OQ-36 的 ⚠️ | spec-board-clock.md 變更紀錄 2026-09-18（看板時間欄位新增） | 自動決議 |
| OQ-55 | 2026-09-18 | canvas-layout | [Level: canvas-layout/s-canvas（跨模組，推翻 OQ-37，影響 F04 時鐘控制畫面）] 【推論】人工於本輪確認：延續 OQ-49 的畫布 item 模式，`spec-canvas-layout.md` 簡介『元件本身的內容與行為（例如圖表的資料、設定、互動）不屬於本模組，由元件所屬模組定義』，人工進一步確認畫布上的 item（看板本體、看板成員、各儀表板）預設對所有能存取該 Board 的人可見，角色差異只影響能不能操作、不影響看不看得到；非 Owner（`r-board-member`）因此仍看得到看板時間目前值與狀態，只是操作被拒絕或不可用。推翻 OQ-37（原「維持討論中，不預設，因無 Scenario 佐證」）。 | 1. 維持 OQ-37：`r-board-member` 完全看不到本畫面 2. `r-board-member` 看得到看板時間目前值與狀態，僅操作被拒絕（本列採用） 3. 沿用 spec 原文，維持討論中不預設 | 採用選項 2：F04 時鐘控制畫面角色與權限表新增 `r-board-member` 一列（可見不可操作）；「無權限」狀態改寫為說明操作被拒絕但仍可檢視；移除 OQ-37 的 ⚠️ | `spec-canvas-layout.md` 簡介；OQ-49 | 自動決議 |
